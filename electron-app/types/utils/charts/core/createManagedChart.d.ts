/**
 * Create a managed Chart.js instance and register it globally.
 *
 * Callers are responsible for error handling and logging so that
 * messages can remain contextual to the specific chart utility.
 *
 * @param {HTMLCanvasElement} canvas - Canvas element to render into.
 * @param {any} config - Chart.js configuration object.
 * @returns {any} The created Chart instance, or null if Chart is undefined.
 */
export function createManagedChart(canvas: HTMLCanvasElement, config: any): any;
//# sourceMappingURL=createManagedChart.d.ts.map
