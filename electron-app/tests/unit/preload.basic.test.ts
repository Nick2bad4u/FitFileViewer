import { describe, it, expect } from 'vitest';

// Minimal placeholder to satisfy Vitest runner. Real tests reside in other preload suites.
describe('preload.basic.test.ts placeholder', () => {
	it('should load test file', () => {
		expect(true).toBe(true);
	});
});
