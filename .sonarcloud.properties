# SonarQube Cloud automatic analysis configuration.
# Keep generated docs, packaged output, fixtures, and tests out of source metrics.
# This project is associated with the built-in "Sonar way" Clean as You Code gate;
# do not gate the first TypeScript release on inherited overall violation counts.
sonar.sources=electron-app,static/app
sonar.tests=tests
sonar.sourceEncoding=UTF-8
