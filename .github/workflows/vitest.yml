name: Vitest

on:
    push:
        paths-ignore:
            - "*"
            - "*/**"
            - "!electron-app/**"
            - "!electron-app"
            - "**/CHANGELOG.md"
            - "CHANGELOG.md"
    pull_request:
        paths-ignore:
            - "*"
            - "*/**"
            - "!electron-app/**"
            - "!electron-app"
            - "**/CHANGELOG.md"
            - "CHANGELOG.md"
    workflow_dispatch:

permissions:
    contents: read
    # Uncomment the next line to enable OIDC for tokenless Codecov uploads
    # id-token: write

concurrency:
    group: vitest-${{ github.ref }}
    cancel-in-progress: false

jobs:
    test:
        name: Run unit tests with coverage
        runs-on: windows-latest
        defaults:
            run:
                working-directory: electron-app
        steps:
            - name: Harden Runner
              uses: step-security/harden-runner@f4a75cfd619ee5ce8d5b864b0d183aff3c69b55a # v2.13.1
              with:
                  egress-policy: audit

            - name: Checkout code
              uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

            - name: Setup Node.js
              uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
              with:
                  node-version: 24

            - name: Cache node modules
              uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
              with:
                  path: electron-app/node_modules
                  key: ${{ runner.os }}-electron-app-node-modules-${{ hashFiles('electron-app/package-lock.json') }}
                  restore-keys: |
                      ${{ runner.os }}-electron-app-node-modules-
              continue-on-error: true

            - name: Install dependencies
              run: |
                  npm ci
              continue-on-error: true

            - name: Run vitest with coverage
              run: |
                  npm run test:ci -- --reporter=junit --outputFile=test-report.junit.xml
              continue-on-error: true

            - name: Upload coverage reports to Codecov
              run: |
                  bash <(curl -s https://codecov.io/bash) -t ${{ secrets.CODECOV_TOKEN }} -f ./coverage/lcov.info -F electron_app -F unit

            - name: Upload test results to Codecov
              shell: cmd
              run: |
                  chcp 65001
                  codecov.exe test-results --token ${{ secrets.CODECOV_TOKEN }} --file test-report.junit.xml
              env:
                  PYTHONIOENCODING: utf-8
                  PYTHONUTF8: 1

            - name: Upload coverage artifact
              uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
              if: always()
              with:
                  name: coverage
                  path: electron-app/coverage
