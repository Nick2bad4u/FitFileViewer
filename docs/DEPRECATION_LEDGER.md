# Deprecation Ledger

This ledger tracks compatibility surfaces that are intentionally temporary. New code should not depend on these surfaces unless the entry explicitly says that the migration still requires it.

## Renderer Global Data Bridge

- Temporary surface: `window.globalData`, `globalThis.globalData`
- Current owner: none; active FIT raw-data reads and writes use `electron-app/utils/state/domain/activeFitRawDataState.ts`
- Compatibility callers: none; runtime FIT-data reads and writes use managed state APIs
- Current status: removed; no automatic runtime bridge install, no state-backed bridge helper, no passive plain-value global fallback, no `globalDataStore` helper, and no `loadedFitFilesState` legacy global mirror or reset-time `loadedFitFiles` global cleanup remain; runtime FIT data load/display paths now write explicit `fitFile.rawData`, and legacy `globalData` writes are unsupported with no preserved state-integration compatibility coverage; AppActions clear/load fallback paths now write explicit `fitFile.rawData`, unified state facade `globalData` access is now an unsupported legacy path instead of routing to active FIT raw data, AppSelectors data-presence checks, chart tab integration readiness, chart state-manager readiness, chart render readiness/state-view/direct-rerender/rendered-event helpers, chart devtools data presence, chart settings rerender guards, chart availability counts, tab visibility no-data checks, tab switching readiness checks, UI active-file display state, map lap selector controls, and GPX export records read through `FitFileSelectors`, chart status refreshes subscribe to state, Playwright smoke assertions read activity counts through renderer state APIs, and lifecycle export/reload handlers, summary storage-key, summary column-modal preference lookup, user/device info, field-toggle metrics, tooltip formatting, data-point filter statistics, event-message charts, lap-zone charts, map lap drawing, map core rendering, elevation profile fallback data and labels, renderer state integration tab loaders, sensor debug utilities, overlay loading, shown-files list rendering/removal, map overlay drawing, elevation profiles, and GPX export record and naming flows now read loaded FIT data through managed state; active FIT activity message reads can use `electron-app/utils/state/domain/fitActivityDataState.ts` for explicit record/session/lap slices, active file identity reads can use `electron-app/utils/state/domain/activeFitFileMetadataState.ts` for current-file/cached-path/loaded-file fallback resolution, map overlay drawing uses `loadedFitFilesState` overlay selectors instead of local loaded-file list slicing, chart readiness/render gating can use `electron-app/utils/state/domain/fitChartDataState.ts` for chart-ready record data, activity start time, and readiness checks, raw-data table rendering can use `electron-app/utils/state/domain/fitTableDataState.ts` for table-ready entries, ordering, row filtering, and readiness checks, user/device rendering can use `electron-app/utils/state/domain/fitUserDeviceDataState.ts` for profile and device-message slices, and map route/add-overlay readiness can use `electron-app/utils/state/domain/fitRouteDataState.ts` for coordinate-bearing records and route coordinate counts; `FitFileSelectors` now read active raw FIT data only from the explicit `fitFile.rawData` domain slice instead of falling back to compatibility `globalData`; `mainUiGlobals`, `main-ui.ts`, state integration startup, core state-manager startup, and the legacy app-state domain no longer own bridge installers
- Unified state facade test-global migration status: unified state manager tests now prove the retired `globalData` facade is blocked and stays absent without deleting `globalData` from `globalThis`.
- Active FIT raw-data message access migration status: `activeFitRawDataState.ts` now reads decoded message arrays through an own-property helper after explicit raw-data narrowing instead of casting active raw-data payloads to a broad string-indexed record.
- State manager root-store migration status: `stateManagerDefaults.ts` now owns only default-state factories and state shape types; the mutable root container moved behind `stateManagerStore.ts`, and `stateManager.ts`/`stateManagerReset.ts` access it through explicit store functions instead of importing an exported `AppState` singleton.
- Next removal step: keep the removed bridge covered by architecture tests while callers continue using typed state APIs
- Verification gates:
  - `npm run lint:app`
  - `npm test -- tests/unit/packaging/architectureBoundaries.test.ts`
  - `npm run test:playwright`
- Exit criteria:
  - Runtime source reads loaded FIT data through typed imports or state selectors.
  - Playwright smoke tests assert state through app APIs instead of `window.globalData`.
  - Architecture tests block direct runtime writes, all runtime reads through `getState("globalData")`, direct `globalData` property definitions, reactive `globalData` property creation, and bridge installer calls.
  - Architecture tests block `globalDataStore`, `getGlobalData`, and `setGlobalData` from runtime source.
  - Architecture tests block direct runtime `loadedFitFiles` global lookups outside `loadedFitFilesState`.
  - Migrated rendering helpers stay free of direct `window.globalData` reads.
  - Active-tab content lookup, active class/ARIA fallback queries, active tab-button lookup, and flexible content ID lookup now route through `electron-app/utils/rendering/helpers/getActiveTabContentRuntime.ts` instead of querying `document` directly inside `getActiveTabContent.ts`; the production document default now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes must provide document access through that provider instead of direct properties or broad `globalThis` fallback scopes.
  - Flexible element ID lookup now checks `HTMLElement` values through `electron-app/utils/ui/dom/elementIdUtilsRuntime.ts` instead of using the ambient constructor inside `elementIdUtils.ts`; the production `HTMLElement` constructor default now reuses the shared browser runtime provider and browser-runtime type alias instead of a local inline `globalThis` getter, and explicit runtime scopes must provide the constructor through that provider instead of direct properties or broad `globalThis` fallback scopes.
  - Tab-button state and debug runtime provider contracts now reuse shared browser-runtime timer and AbortController aliases instead of spelling direct ambient timer/controller types, with architecture coverage blocking those direct type spellings from returning.
  - No runtime `getGlobalData`/`setGlobalData` compatibility helper remains.

## Legacy AppState Global

- Temporary surface: `AppState.globalData`, `AppState.eventListeners`, `AppState.isChartRendered`, `window.isChartRendered`
- Current owner: `electron-app/utils/state/integration/stateIntegration.ts`
- Compatibility callers: none
- Current status: removed; state integration no longer exposes `AppState.globalData`, non-FIT `AppState` accessors, or `window.isChartRendered`, state integration preserves pre-existing plain `globalData` values into managed state without installing a global accessor, the core state manager no longer exports a generic `createReactiveProperty` global binding helper, the legacy renderer-domain `appState` manager has been removed, the unified state facade no longer exposes legacy path routing or synchronization, and architecture tests block reintroducing runtime global data access paths
- Next removal step: keep the removed global accessors covered by architecture tests while callers use `getState(...)`/`setState(...)`, typed state services, or development-only debug utilities
- Verification gates:
  - `npm test -- tests/unit/utils/state/integration/stateIntegration.simple.test.ts tests/unit/utils/state/integration/stateIntegration.comprehensive.test.ts`
  - `npm test -- tests/unit/packaging/architectureBoundaries.test.ts`
- Exit criteria:
  - Renderer modules use `getState`, `setState`, or typed domain services directly.
  - No runtime module writes global data through `AppState.globalData`.
  - Non-FIT `AppState` and `window.isChartRendered` compatibility accessors stay removed.

## Renderer Utility Globals

- Temporary surface: `rendererUtils` and broad renderer utility global registration
- Current owners: none for the broad `utils.js`/`FitFileViewerUtils` utility bag; remaining migration work is feature-scoped runtime/vendor adapter cleanup, not a shared renderer utility global bridge
- Compatibility callers: none for the broad utility bag
- Current status: retired for broad renderer utility globals; renderer bootstrap now has focused modules for app startup, app lifecycle wiring, DOM element access, state manager startup, file input startup and entrypoint wiring with listener error isolation, test-only listener wiring with registration error isolation, import-time setup orchestration, import-time state/theme/listener bootstrap, renderer diagnostics wiring, development debug tools, Electron API wiring, Electron API registration, Electron menu action handlers, core module resolution, error handling, lifecycle cleanup, notifications, loading, and renderer state bindings; the compatibility-only `rendererUtils` barrel, global `setLoading` bridge, formatting utility globals, global `copyTableAsCSV` bridge, root `utils.js` bridge, `FitFileViewerUtils`, `devUtilsHelpers`, `window.showFitData`, renderer app-info/export-button/reset globals, `electron-app/renderer/globalApiExposure.ts`, `electron-app/utils/ui/mainUiGlobals.ts`, and `electron-app/utils/legacy/` registry have been removed and are covered by an explicit deleted-file architecture guard; Alt FIT file handoff now goes through the typed `sendFitFileToAltFitReader` service instead of `window.sendFitFileToAltFitReader`; chart settings, chart resize, inline zone-color controls, and zone-color picker fallbacks now call typed `renderChartJS` imports instead of probing `window.renderChartJS`; chart theme and resize refreshes now use the typed chart updater instead of `window.ChartUpdater` or `window.chartUpdater`; map overlay highlight state now lives in typed `getHighlightedOverlayIndex`/`setHighlightedOverlayIndex` module state instead of `_highlightedOverlayIdx` and global `updateOverlayHighlights` accessors; marker-count state now lives in `electron-app/utils/maps/state/mapMarkerCountState.ts` instead of `window.mapMarkerCount`; map action retry and highlight bookkeeping now uses module state instead of `__centerMainAttempts`, `__centerRetryHandle`, `__centerStatusNotified`, or `__mainPolylineHighlightToken` globals; map measurement-control registration and Clear All cleanup now go through `electron-app/utils/maps/state/mapMeasureControlState.ts` instead of `_measureControl`; Leaflet map-instance registration, removal, tab invalidation, and overlay/action centering now go through `electron-app/utils/maps/state/mapLeafletInstanceState.ts` instead of `_leafletMapInstance`; shown-file list refresh now stays behind the typed `shownFilesListUpdater` service instead of being assigned to `window.updateShownFilesList`, and shown-file list DOM creation, tooltip queries, and body appends now route through `electron-app/utils/rendering/components/shownFilesListRuntime.ts`; lifecycle CSV export now imports the CSV serializer directly; overlay loading now refreshes maps through a typed dynamic import; regular file open, path-based file open, recent-file menu loading, and decoder-option reloads now render decoded FIT data through a typed lazy import; `showFitData`, tab state handlers, the map estimated-power refresh path, overlay loading, marker-count controls, map core refreshes, overlay highlight refreshes, and active filename setup now import chart/tab/rendering/file-list/map helpers directly instead of waiting for utility globals; state domain/core modules and state integration are guarded against importing broad renderer utilities; and `renderer.ts` now uses focused app startup, app lifecycle wiring, DOM access, state startup, file-input wiring, test-only bootstrap wiring, import-time setup orchestration, renderer diagnostics wiring, debug tool creation, Electron API wiring, and loading helpers directly without carrying debug-only state-manager helper code
- Renderer environment input migration status: development-mode detection now names the preload bridge value as an `electronApiCandidate` provided by `getElectronApiCandidate`, and architecture coverage blocks the old direct `electronAPI` environment input/provider shape from returning.
- Embedded AltFit bridge migration status: `static/ffv/electron-altfit-bridge.js` keeps base64 handoff and loader handling local to the bridge script instead of publishing `window.handleFitFileBase64`; the embedded wrapper no longer installs an `window.electronAPI.openFileDialog` fallback shim, no longer probes `window.loadFitFileFromArrayBuffer` or `window.ffvApp`, and now hands files to the embedded AltFit app through a direct file-input retry path; the generated AltFit bundle no longer publishes its own `window.loadFitFileFromArrayBuffer` automation global.
- Renderer diagnostics migration status: development diagnostics now accept an explicit local debug-core dependency contract instead of deriving from `RendererCoreModules`, type the debug/dev tool views, and call state-manager debug methods through a local shape guard instead of accepting broad module records or record-typed debug tool bags.
- Renderer file-input wiring migration status: file-input startup no longer accepts the renderer entrypoint's generic `toModuleRecord` helper; test override normalization stays local to `fileInputWiring.ts` through a named handle-open override-module shape instead of a broad string-indexed record helper, and production file opening now uses the locally owned typed `RendererFileInputCoreModules.handleOpenFile` dependency instead of accepting or deriving from the whole `RendererCoreModules` object.
- FIT current-file state migration status: active file identity now lives only in `fitFile.currentFile`; `FitFileStateManager`, renderer active-file helpers, and app lifecycle actions no longer write the retired top-level `currentFile` mirror, and the canonical default app-state shape does not include that legacy branch.
- Map polyline migration status: main/overlay map polyline registration and stored main bounds now go through `electron-app/utils/maps/state/mapPolylineRegistryState.ts` instead of `_mainPolyline`, `_mainPolylineOriginalBounds`, or `_overlayPolylines`.
- Map activity-layer migration status: activity-layer group registration and data-point marker tracking now go through `electron-app/utils/maps/state/mapActivityLayerState.ts` instead of `_ffvActivityLayerGroup` or `_ffvDataPointMarkers`.
- Map draw-laps test window fixture migration status: map draw-laps tests now install and restore the temporary jsdom `window` through a captured descriptor instead of assigning or deleting `testGlobal.window` directly.
- Map draw-laps runtime migration status: overlay fit-bounds retry scheduling, cleanup, marker DOM creation, text-node creation, and marker SVG element checks now route through `electron-app/utils/maps/layers/mapDrawLapsRuntime.ts` instead of calling timer globals, creating through `document`, or checking `SVGElement` directly inside `mapDrawLaps.ts`; production timer, document, and SVGElement defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers and timer/SVGElement type aliases, production defaults stay behind named provider scope functions instead of a broad `globalThis` default scope, and explicit runtime scopes must use those providers instead of direct timer, document, or SVGElement properties.
- Map data-point filter migration status: filter config and last summary result now go through `electron-app/utils/maps/state/mapDataPointFilterState.ts` instead of `mapDataPointFilter` or `mapDataPointFilterLastResult` globals.
- Data-point filter state-helper naming migration status: filter helper APIs and tests now refer to active FIT records and typed map filter state instead of preserving `global`/`window` wording around the already-migrated `mapDataPointFilterState` storage path.
- Active main map-file migration status: the selected main-file index now goes through `electron-app/utils/maps/state/mapActiveMainFileState.ts` instead of `_activeMainFileIdx`.
- Add-FIT-overlay button migration status: overlay button availability refresh and the single FIT-data subscription now go through `electron-app/utils/ui/controls/addFitOverlayButtonState.ts` instead of `__ffvAddFitOverlayButtonUpdate` or `__ffvAddFitOverlayButtonUnsubscribe`.
- Map theme toggle migration status: toggle refresh listeners and the current updater now go through `electron-app/utils/theming/specific/mapThemeToggleState.ts` instead of `__ffvMapThemeToggleListenersController`, `__ffvMapThemeToggleListenersInstalled`, or `__ffvMapThemeToggleUpdate`. Delayed toggle refresh scheduling, cleanup, listener abort-controller creation, button listener abort-controller creation, existing-toggle lookup, button/span/SVG/fallback DOM creation, body theme-class reads, map-theme change event creation/dispatch, and document listener registration now route through `electron-app/utils/theming/specific/mapThemeToggleRuntime.ts` instead of calling timer globals, constructing `AbortController` or `CustomEvent`, querying or dispatching document events, creating elements, reading `document.body`, or registering document listeners directly inside the map theme toggle modules; production defaults and explicit runtime scopes stay behind named provider scope functions instead of a broad `globalThis` default scope or direct scope properties, map-theme toggle production AbortController, CustomEvent, document, and timer lookups plus update-map-theme production AbortController, document, and HTMLElement lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters or direct ambient constructor/timer types, and explicit runtime scopes must provide their browser and timer primitives through those providers. The direct `window.updateMapTheme` callback path is removed; map tile updates now rely on the typed `mapThemeChanged` event listener installed by `updateMapTheme.ts`, and that listener's abort-controller creation now routes through `electron-app/utils/theming/specific/updateMapThemeRuntime.ts` instead of constructing `AbortController` directly inside `updateMapTheme.ts`; update-map-theme document access, HTMLElement checks, beforeunload listener target, and abort-controller creation now stay behind named provider scope functions and shared AbortController/HTMLElement constructor aliases instead of a broad runtime scope, direct ambient constructor type spellings, direct scope properties, or partial `window` scope.
- Core theme runtime migration status: theme-transition class add/removal, body theme-class updates, body/document theme data attributes, body computed-style reads, theme-change document/body/browser event-target access, theme-change event construction, theme-transition style injection, mobile meta theme-color updates, persisted theme storage reads/writes/legacy-key cleanup, system-theme media-query lookup, and system-theme listener abort-controller creation now route through `electron-app/utils/theming/core/themeRuntime.ts` instead of probing `globalThis.window`, probing `globalThis.matchMedia`, querying or creating through `document` directly, reading or mutating `document.body` directly, calling timer globals, reading or mutating `localStorage` directly, constructing `AbortController`, or constructing `CustomEvent` directly inside `theme.ts`; production defaults and explicit runtime scopes stay behind named provider scope functions instead of a broad `globalThis` default scope or direct scope properties, theme-runtime production AbortController, timer, computed-style, CustomEvent, document, storage, media-query, and browser event-target lookups now reuse shared browser runtime providers and type aliases instead of local inline `globalThis` getters, direct ambient browser API types, a partial `window`, or the retired `getGlobalEventTarget` theme provider, and the default media-query provider lives on the explicit runtime scope as a bound browser function without helper-level ambient `matchMedia` captures, a module-level `browserGlobal` alias, or a direct optional browser-global shape.
- Setup theme runtime migration status: main-process theme fetch timeout scheduling and cleanup plus setup-time persisted theme reads/writes/legacy-key cleanup now route through `electron-app/utils/theming/core/setupThemeRuntime.ts` instead of calling timer globals or reading/mutating `localStorage` directly inside `setupTheme.ts`; production timer and storage defaults now reuse shared browser runtime providers and timer type aliases, and production defaults plus explicit runtime scopes stay behind named provider scope functions instead of a broad `globalThis` default scope, direct timer/storage properties, or direct ambient timer API types.
- Map document listener migration status: basemap panel layout refs, map-type button refs, zoom-dragging refs, document listener lifecycle, and the render abort controller now stay in typed module state instead of `__ffvLayoutLayersControl`, `__ffvMapTypeButton`, `__ffvMapZoomDraggingRef`, `__ffvMapDocumentListenersController`, `__ffvMapDocumentListenersInstalled`, or `__ffvRenderMapAbortController`; listener abort-controller creation, layers-control lookup, HTMLElement/Node checks, and document/resize listener registration now route through `electron-app/utils/maps/core/mapDocumentListenersRuntime.ts` instead of constructing controllers, querying `document`, checking browser constructors, or registering document/window listeners directly inside `mapDocumentListeners.ts`; production AbortController, document, HTMLElement, Node, and resize-target defaults now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, and explicit runtime scopes must now provide abort-controller, document, HTMLElement, Node, and resize-target access through those providers instead of falling back to direct scope properties, a broad runtime scope, or partial `window` scope.
- Map plugin-control migration status: Leaflet.draw control, drawn-items, and minimap control refs now stay in `electron-app/utils/maps/state/mapPluginControlState.ts` instead of `_drawControl`, `_drawnItems`, or `_miniMapControl` globals.
- File-browser library cache migration status: decoded library scan payloads now go through `electron-app/utils/ui/browser/fileBrowserLibraryCache.ts` instead of the `__ffvLibraryCache` global.
- Startup initializer runtime migration status: `electron-app/utils/ui/initStartup.ts` now resolves its DOMContentLoaded event target through `electron-app/utils/ui/initStartupRuntime.ts` instead of reading the global document directly; production document access now reuses the shared `electron-app/utils/runtime/browserRuntime.ts` provider, default document access stays behind the named default provider instead of a private `getGlobalDocument()` helper, and explicit runtime scopes must use the provider instead of a direct `documentTarget` property.
- Overlay selector loader migration status: browser file-input overlay loading now calls the typed `loadOverlayFiles` import directly instead of supporting test-time `window.loadOverlayFiles` injection.
- Overlay loader runtime migration status: overlay file load concurrency and active-tab preservation now read browser metadata and DOM state through `electron-app/utils/files/import/loadOverlayFilesRuntime.ts` instead of probing `globalThis.navigator` or querying `document` directly inside `loadOverlayFiles.ts`; production document and navigator lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, and production defaults plus explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct `document`/`navigator` properties.
- Single overlay file runtime migration status: Blob `Response` reads, FileReader fallback construction, and FileReader listener abort-controller creation now route through `electron-app/utils/files/import/loadSingleOverlayFileRuntime.ts` instead of constructing `Response`, `FileReader`, or `AbortController` directly inside `loadSingleOverlayFile.ts`; production AbortController, FileReader, and Response lookups now reuse shared browser runtime providers and shared constructor aliases instead of local inline `globalThis` getters or direct ambient constructor types, production defaults and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct `Response`/`FileReader`/`AbortController` properties, FileReader fallback tests inject the runtime scope instead of mutating globals, overlay decoder tests, including FIT-payload regressions, now pass an explicit Electron API scope into `loadSingleOverlayFile` instead of registering the renderer fallback candidate, and overlay decoder candidate typing now uses an explicit shared file API-domain method alias instead of deriving from the monolithic `ElectronAPI` type or a local Electron API interface.
- Open-FIT-file path runtime migration status: button element checks now resolve the HTMLElement constructor through `electron-app/utils/files/import/openFitFileFromPathRuntime.ts` instead of checking the ambient constructor directly inside `openFitFileFromPath.ts`; the production HTMLElement default now reuses the shared browser runtime provider and browser-runtime type alias instead of a local inline `globalThis` getter, explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct `HTMLElement` property, and file-path opener tests now pass an explicit Electron API scope into `openFitFileFromPath` instead of registering the renderer fallback candidate. File-path opener Electron API candidate typing now uses explicit shared file and preload-event API-domain method aliases instead of deriving from the monolithic `ElectronAPI` type or a local Electron API interface.
- Open-file selector runtime migration status: hidden overlay input creation, jsdom detection, microtask fallback, timeout cleanup, and abort-controller creation now route through `electron-app/utils/files/import/openFileSelectorRuntime.ts` instead of calling browser primitives or constructing `AbortController` directly inside `openFileSelector.ts`. Production defaults and explicit open-file selector runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct scope properties, or direct ambient primitive types; production document, navigator, microtask, AbortController, and timer lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, explicit scopes must now provide document, navigator, microtask, timer, and controller primitives through providers instead of falling back to `globalThis`, and native overlay-dialog tests now pass an explicit Electron API scope into `openFileSelector` instead of registering the renderer fallback candidate. Overlay selector Electron API candidate typing now uses explicit shared dialog and file API-domain method aliases instead of deriving from the monolithic `ElectronAPI` type or a local Electron API interface.
- Handle-open-file Electron API migration status: `handleOpenFile` and `validateElectronAPI` can now receive an explicit Electron API scope for file dialog, read, and parse bridge access; the FIT decode-payload regression, complete behavior tests, and colocated handle-open-file coverage now use scoped providers instead of registering the renderer fallback candidate, and handle-open-file candidate typing now uses explicit shared dialog and file API-domain method aliases instead of deriving from the monolithic `ElectronAPI` type or a local Electron API interface.
- Handle-open-file window fixture migration status: colocated handle-open-file coverage no longer installs a placeholder `global.window` object because Electron API access is covered through explicit scoped providers.
- Sensor debug test-global migration status: debug sensor unit tests now prove active FIT state drives sensor data availability without defining or deleting a stale `globalData` property on `globalThis`.
- Loaded FIT files test-global migration status: loaded-file state unit tests now prove explicit `fitFile.loadedFiles` storage without assigning or deleting the retired `loadedFitFiles` global.
- Overlay tooltip timeout migration status: delayed overlay filename tooltip cleanup now stays in `electron-app/utils/rendering/components/shownFilesListTooltipState.ts` module state instead of `_overlayTooltipTimeout` on the global object, and shown-files list tooltip/highlight timer scheduling, cleanup, listener abort-controller creation, body theme-listener registration, tooltip mousemove registration, and viewport reads now route through `electron-app/utils/rendering/components/shownFilesListRuntime.ts` instead of calling timer, listener, controller, or viewport globals directly inside the shown-files list modules; shown-files list production AbortController lookup now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes must now provide document body, mousemove event-target, viewport, controller, and timer primitives instead of falling back to `globalThis`.
- Fullscreen button listener migration status: keydown and native fullscreen-change handler refs now stay in `electron-app/utils/ui/controls/addFullScreenButton.ts` module state instead of `__ffvFullscreenKeydownHandler` or `__ffvNativeFullscreenChangeHandler`; listener abort-controller creation, window/document listener registration/removal, fullscreen button DOM creation/body append, loaded-file body-state reads, and body class observation now route through named providers in `electron-app/utils/ui/controls/addFullScreenButtonRuntime.ts` instead of constructing `AbortController`, calling event-target globals directly, creating/appending through `document`, reading or observing `document.body` directly inside `addFullScreenButton.ts`, carrying a partial `window` target scope, or accepting direct-property runtime fallbacks; fullscreen-button production AbortController, document, window event-target, HTMLElement, KeyboardEvent, and MutationObserver lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, the fullscreen-button controller/element/keyboard/mutation-observer provider contracts now reuse shared browser-runtime constructor aliases instead of direct ambient constructor type spellings, and the deprecated `setupDOMContentLoaded` alias has been removed in favor of `setupFullscreenListeners`.
- Fullscreen button Electron API migration status: `addFullScreenButton` and `setupFullscreenListeners` now accept an explicit renderer Electron API scope, so fullscreen IPC tests no longer register a shared renderer fallback candidate for `setFullScreen`; scoped fullscreen API validation now uses an explicit optional-function property guard instead of casting the candidate preload API to a generic record, and its candidate typing now uses the shared menu-event API-domain contract instead of deriving from the monolithic `ElectronAPI` type.
- Settings modal Electron API migration status: `showSettingsModal` now accepts an explicit renderer Electron API scope for main-process theme synchronization, so settings modal tests no longer register a shared renderer fallback candidate for `sendThemeChanged`, and theme-change IPC candidate typing now uses the shared menu-event API-domain contract instead of deriving from the monolithic `ElectronAPI` type.
- Keyboard shortcuts modal Electron API migration status: `showKeyboardShortcutsModal` now accepts an explicit renderer Electron API scope and forwards it to external-link handling, so keyboard shortcuts modal tests no longer register a shared renderer fallback candidate for `openExternal`.
- Renderer environment Electron API migration status: development-mode detection now reads the explicit `electronAPI` value from `RendererEnvironmentInput` instead of consulting the shared registered renderer Electron API fallback; scoped dev-mode inspection now uses object and property guards instead of casting the candidate preload API to a generic record.
- Update notification Electron API migration status: `showUpdateNotification` now accepts an explicit renderer Electron API scope for restart-and-update actions, so update-notification tests no longer register a shared renderer fallback candidate for `installUpdate`; scoped update API validation now uses an explicit optional-function property guard instead of casting the candidate preload API to a generic record, and its candidate typing now uses the shared menu-event API-domain contract instead of deriving from the monolithic `ElectronAPI` type.
- Import-time listener Electron API migration status: import-time renderer listener setup now receives the entrypoint's explicit renderer Electron API scope before wiring lifecycle IPC, so early listener registration no longer depends on the shared registered renderer Electron API fallback.
- Renderer Electron API registration migration status: renderer Electron API registration now only wires preload startup hooks, the shared registered renderer Electron API fallback runtime has been removed, and production renderer code must receive explicit Electron API scopes from the entrypoint instead.
- Main UI Electron API contract migration status: `mainUiElectronApi.ts` now defines `MainUiElectronApi` as an explicit interface over split shared API-domain contracts instead of deriving the candidate type with `Partial<Pick<ElectronAPI, ...>>`; candidate validation now uses explicit optional-function property checks instead of casting the preload candidate to a broad record.
- About modal Electron API migration status: `showAboutModal` now accepts an explicit renderer Electron API scope and forwards it to external-link handling, so about modal tests no longer register a shared renderer fallback candidate for `openExternal`.
- File browser Electron API migration status: `renderFileBrowserTab` now accepts an explicit renderer Electron API scope for folder listing, reading, and library scanning, so file-browser accessibility tests no longer register a shared renderer fallback candidate for Browser IPC methods; scoped Browser IPC validation now uses explicit optional-function property guards instead of casting the candidate preload API to a generic record or probing method names with `Reflect.get`, and its candidate typing now composes shared file, dialog, and Fit Browser API-domain contracts with explicit method slices instead of deriving from the monolithic `ElectronAPI` type or local `Pick<FitBrowserElectronAPI, ...>` aliases.
- Theming Electron API migration status: `setupTheme` and `listenForThemeChange` now accept explicit renderer Electron API scopes for theme fetch/listener IPC, so theming tests no longer register shared renderer fallback candidates for `getTheme`, `onSetTheme`, or `sendThemeChanged`; scoped theming API validation now uses explicit optional-function property guards instead of casting candidate preload APIs to generic records, setup-theme fetch candidate typing now uses an explicit readonly method alias backed by the shared theme API-domain contract, and live theme-change IPC candidate typing now uses shared menu-event API-domain contracts instead of deriving from the monolithic `ElectronAPI` type or carrying local Electron API interfaces.
- FIT data display Electron API migration status: `showFitData` now accepts an explicit renderer Electron API scope for file-loaded IPC notification, so strict FIT data rendering tests no longer register a shared renderer fallback candidate for `notifyFitFileLoaded`; scoped FIT-data API validation now uses an explicit optional-function property guard instead of casting the candidate preload API to a generic record, and file-loaded notification candidate typing now uses a named readonly alias backed by the shared preload-event API-domain contract instead of deriving from the monolithic `ElectronAPI` type or carrying a generic local interface.
- Recent-files context menu Electron API migration status: `attachRecentFilesContextMenu` now accepts an explicit renderer Electron API scope for recent-file listing, reading, parsing, and recency updates, so recent-files context menu tests no longer register a shared renderer fallback candidate, and recent-file menu candidate typing now uses explicit readonly method aliases backed by the shared file API-domain contract instead of deriving from the monolithic `ElectronAPI` type or carrying a local `Omit`/`Partial<Pick>` bridge.
- Lifecycle listener Electron API migration status: `setupListeners` now accepts an explicit renderer Electron API scope and forwards it to lifecycle IPC resolution, recent-files context menu registration, and menu IPC registration, so FIT-payload listener tests no longer register a shared renderer fallback candidate; scoped lifecycle API validation now uses explicit optional-function property guards instead of casting the candidate preload API to a generic record.
- Lifecycle listener unit fixture migration status: the main lifecycle listener suite and legacy `utils/app/events` listener alias suite now supply explicit renderer Electron API scopes for open-file, recent-file, menu IPC, and keyboard-shortcut modal paths instead of registering shared renderer fallback candidates.
- Strict lifecycle listener fixture migration status: the strict lifecycle listener suite now injects a scoped renderer Electron API provider through a local `setupListeners` wrapper instead of registering the shared renderer fallback candidate for each fixture.
- Renderer state integration Electron API migration status: `initializeRendererWithNewStateSystem` now accepts an explicit renderer Electron API scope for file-opened IPC integration, so renderer state integration tests no longer register the shared renderer fallback candidate, and file-opened IPC candidate typing now uses an explicit shared preload-event API-domain method alias instead of deriving from the monolithic `ElectronAPI` type or a local Electron API interface.
- Master state manager Electron API migration status: `MasterStateManager` now accepts an explicit renderer Electron API scope for app version, development-mode, and keyboard shortcut file-open IPC paths, so the comprehensive master state manager suite no longer registers the shared renderer fallback candidate; scoped master-state API validation now uses explicit optional-function and development-mode property guards instead of casting the candidate preload API to a generic record.
- Main-process state client Electron API migration status: `MainProcessStateClient` now accepts an explicit renderer Electron API scope for main-state IPC reads, writes, diagnostics, and listeners, so client tests cover scoped operation without relying on the shared renderer fallback candidate; scoped main-state API validation now uses explicit required-function property guards instead of casting the candidate preload API to a generic record, and main-state client candidate typing now uses explicit readonly method aliases backed by the shared main-state API-domain contract instead of deriving from the monolithic `ElectronAPI` type or a local `Pick` bridge.
- Renderer startup hooks Electron API migration status: `getElectronApiStartupHooks` now requires an explicit renderer Electron API scope and no longer reads the shared registered fallback candidate when called without one.
- Menu IPC listener Electron API migration status: `registerMenuIpcListeners` now accepts an explicit renderer Electron API scope for menu event registration and scoped modal forwarding, so keyboard-shortcuts menu IPC tests no longer register a shared renderer fallback candidate, and menu IPC listener candidate typing now uses explicit readonly method aliases backed by the shared menu-event API-domain contract instead of deriving from the monolithic `ElectronAPI` type or carrying a local `Partial<Pick>` bridge; scoped menu IPC API validation now uses direct optional-function property guards instead of probing method-name strings with `Reflect.get`.
- Menu IPC unit fixture migration status: the main `registerMenuIpcListeners` unit suite now supplies an explicit renderer Electron API scope for menu event registration and scoped keyboard-shortcut fallback assertions instead of registering a shared renderer fallback candidate.
- Fullscreen overlay and marker-count selector runtime migration status: exit-fullscreen overlay add/remove element checks, overlay abort-controller creation, and marker-count abort-controller/event creation now resolve through explicit runtime scopes and document-window constructors instead of falling back to `globalThis` constructors for custom scopes; fullscreen-overlay and marker-count production defaults stay behind named provider scope functions rather than per-property global getters, marker-count production AbortController, document, and Event lookups plus exit-fullscreen overlay add/remove document lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, exit-fullscreen overlay add/remove AbortController and HTMLElement contracts plus marker-count AbortController and Event contracts now reuse shared browser-runtime aliases instead of direct ambient constructor types, and marker-count plus exit-fullscreen overlay removal scopes no longer accept direct-property runtime fallbacks.
- Add-FIT-map and elevation-profile button runtime migration status: map-action button element creation, popup opening, theme reads, and click-listener abort-controller creation now route through `electron-app/utils/ui/controls/createAddFitFileToMapButtonRuntime.ts` and `electron-app/utils/ui/controls/createElevationProfileButtonRuntime.ts`; production defaults stay behind named provider scope functions rather than per-property global getters, add-FIT-map production AbortController and document lookups plus elevation-profile production document and popup-opener lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, map-action AbortController contracts now reuse the shared browser-runtime alias instead of direct ambient constructor types, elevation-profile default palette access now uses a focused global-property helper instead of casting `globalThis` to a broad feature-specific scope or using generic `Reflect.get(globalThis, ...)` probes, elevation-profile explicit scopes no longer accept direct-property runtime fallbacks, and explicit runtime scopes must provide their own providers instead of falling back to `globalThis`.
- Power-estimation button runtime migration status: map-action button creation and click-listener abort-controller creation now route through `electron-app/utils/ui/controls/createPowerEstimationButtonRuntime.ts`; production defaults stay behind named provider scope functions rather than per-property global getters, production AbortController and document lookups now reuse shared browser runtime providers and the browser-runtime AbortController constructor alias instead of local inline `globalThis` getters or direct ambient constructor types, direct-property runtime fallbacks are no longer accepted, and explicit runtime scopes must provide their own document and abort-controller providers instead of falling back to `globalThis`.
- Zone controls runtime migration status: HR, detailed power, and simple power zone DOM creation, element checks, storage access, and listener abort-controller creation now route through `electron-app/utils/ui/controls/createHRZoneControlsRuntime.ts`, `electron-app/utils/ui/controls/createPowerZoneControlsRuntime.ts`, and `electron-app/utils/ui/controls/createPowerZoneControlsSimpleRuntime.ts`; production defaults now reuse shared browser runtime providers for AbortController, document, HTMLElement, and localStorage lookups instead of local inline `globalThis` getters, zone-control controller and element-constructor contracts now reuse shared browser-runtime aliases instead of direct ambient constructor types, zone controls no longer accept direct-property runtime fallbacks, and explicit runtime scopes must provide their own document, constructor, abort-controller, and storage runtimes instead of falling back to `globalThis`.
- Map fullscreen control runtime migration status: delayed Leaflet map size invalidation after custom fullscreen toggles, listener abort-controller creation, map-container lookup, fullscreen state reads, fullscreen exit, document body containment, old-button cleanup, fullscreen control/button/SVG DOM creation, and document fullscreen-change listener registration now route through `electron-app/utils/maps/controls/mapFullscreenControlRuntime.ts` instead of calling timer, controller, selector, fullscreen, element-creation, or document listener globals directly inside `mapFullscreenControl.ts`; production document, timer, and AbortController lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters or direct ambient timer/controller types, production defaults stay behind named provider scope functions, and explicit runtime scopes must now provide their document, timer, and abort-controller providers instead of direct properties or falling back to `globalThis`.
- Menu IPC forwarder migration status: duplicate-registration tracking for menu Save As and Export forwarding now stays in `electron-app/utils/app/lifecycle/menuIpcListeners.ts` module state instead of `__ffvMenuForwardRegistry`; scoped menu IPC API validation now uses explicit optional-function property checks instead of casting the candidate preload API to a generic record or probing method names reflectively.
- App menu debug/recent-file injection migration status: menu debug logging now uses the `FFV_DEBUG_MENU` environment flag only, and menu recent-file tests use a module-scoped override instead of `__FFV_debugMenu` or `__mockRecentFiles` on the global object.
- Filename auto-scroll migration status: active filename observer, resize, and timer cleanup state now stays in `electron-app/utils/ui/unifiedControlBar.ts` module state instead of `__ffvFilenameAutoScrollState` on the DOM element, and filename auto-scroll resize-listener registration, cleanup timers, cleanup abort-controller creation, document access, element checks, and mutation-observer creation now route through `electron-app/utils/ui/unifiedControlBarRuntime.ts`; production defaults now live behind named provider functions, and explicit runtime scopes must provide controller, document, event-target, element-constructor, observer, and timer providers instead of direct properties or `globalThis` fallbacks.
- Quick color switcher migration status: listener lifecycle and delayed-close timer state now stays in `electron-app/utils/ui/quickColorSwitcher.ts` module state instead of `__ffvQuickColorSwitcherState` on the DOM element.
- Quick color switcher runtime migration status: DOM construction, SVG construction, text-node creation, document query/append operations, scoped node checks, delayed dropdown close scheduling, cleanup, listener abort-controller creation, and outside-click document listener registration now route through `electron-app/utils/ui/quickColorSwitcherRuntime.ts` instead of calling document APIs, timer globals, constructing `AbortController`, or registering document listeners directly inside `quickColorSwitcher.ts`; production defaults stay behind named provider scope functions rather than per-property global getters, quick-color-switcher production AbortController, timer, document, and Node lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, and explicit runtime scopes must use those providers instead of direct controller, document, node, or timer properties.
- About modal runtime migration status: pre-initialization document readiness checks and DOMContentLoaded registration now resolve document access through `electron-app/utils/ui/modals/aboutModalRuntime.ts` instead of probing the global document directly inside `aboutModal.ts`.
- About modal helper document migration status: about modal creation and stylesheet injection now resolve document access through `electron-app/utils/ui/modals/aboutModalRuntime.ts` instead of querying or creating through `document` directly inside `ensureAboutModal.ts` or `injectModalStyles.ts`.
- Map action button migration status: active filename click, keyboard, hover, and cleanup listener state now stays in `electron-app/utils/maps/controls/mapActionButtons.ts` module state instead of `__ffvMapActionCleanup` on the DOM element, and active-file centering retry scheduling/cleanup, center-status and highlight timestamps, active filename/map-tab DOM lookup, DOM readiness checks, DOMContentLoaded registration, and HTMLElement checks now route through `electron-app/utils/maps/controls/mapActionButtonsRuntime.ts`; production timer, clock, document, HTMLElement, KeyboardEvent, and MutationObserver defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers and browser-runtime type aliases, and production defaults and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct timer/date/document/HTMLElement/constructor properties.
- Lifecycle listener cleanup migration status: Open File button listener cleanup now stays in `electron-app/utils/app/lifecycle/lifecycleListenerCleanupRegistry.ts` WeakMap state instead of `__ffvLifecycleListenersCleanup` on the DOM element.
- Map measurement Escape-key migration status: measurement Escape handler replacement and disposal now stays in `electron-app/utils/maps/controls/mapMeasureTool.ts` and `electron-app/renderer/leafletMeasureLite.js` module state instead of `__ffvMapMeasureEscapeHandler` or `__ffvLeafletMeasureLiteEscapeHandler` on the global object, and fallback measure-button re-enable timer scheduling/cleanup, listener abort-controller creation, document keydown listener registration/removal, measurement button/label/SVG element creation, text-node creation, and HTMLElement target checks now route through `electron-app/utils/maps/controls/mapMeasureToolRuntime.ts` and `electron-app/renderer/leafletMeasureLiteRuntime.js` instead of calling timer, controller, document listener, element factory, or constructor globals directly; production timer, AbortController, document, and HTMLElement lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters or direct ambient timer/controller/HTMLElement types, and explicit runtime scopes must now provide document, element-constructor, timer, and abort-controller providers instead of direct properties or falling back to `globalThis`.
- Map lap selector migration status: multi-lap drag mouseup replacement and disposal now stays in `electron-app/utils/maps/controls/mapLapSelector.ts` module state instead of `__ffvLapSelectorMouseupHandler` on the global object, and lap-selector listener abort-controller creation, control/label/option/help element creation, select change-event construction, plus document mouse/key listener registration/removal now route through `electron-app/utils/maps/controls/mapLapSelectorRuntime.ts` instead of constructing controllers or events, creating through `document`, or touching document listeners directly inside `mapLapSelector.ts`; production AbortController, document, and Event defaults now reuse shared browser runtime providers and browser-runtime constructor type aliases instead of local inline `globalThis` getters or direct ambient constructor types, and explicit runtime scopes must now provide document, abort-controller, and Event providers instead of direct properties or falling back to `globalThis`.
- Zone-data migration status: heart-rate and power zone arrays now stay in `electron-app/utils/data/zones/zoneDataState.ts` module state instead of `window.heartRateZones` or `window.powerZones`.
- Generic storage utility migration status: default `localStorage` resolution now routes through `electron-app/utils/storage/storageUtilsRuntime.ts`; ordinary storage helpers keep provider-based access without reading `globalThis` directly, the runtime default reuses `electron-app/utils/runtime/browserRuntime.ts` instead of owning an explicit `globalThis.localStorage` provider or generic `Reflect.get(globalThis, "localStorage")` probe, and explicit runtime scopes must use the storage provider instead of direct `localStorage` properties.
- Main-process environment migration status: main logging, safe menu creation, window-state security toggles, main-window bootstrap test-mode branches, and application event-handler dev/test/Gyazo credential gates now read `NODE_ENV`, argv, and feature flags through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `globalThis.process`, `process.env`, or `process.argv` directly; application event-handler permission-detail string reads now use typed indexed access instead of generic `Reflect.get` probes.
- Menu and main-state environment migration status: app-menu debug gating and main-process state development gating now reuse `electron-app/utils/runtime/processEnvironment.ts` instead of owning per-module `Reflect.get(globalThis, "process")` probes.
- Main UI runtime environment migration status: renderer main-UI startup/unload console and clock access now route through injectable `electron-app/renderer/mainUiRuntimeEnvironment.ts` scope providers instead of forcing tests to patch `globalThis.console` or `Date.now`; main-UI startup now captures the preload API candidate through that runtime environment and passes an explicit Electron API scope to startup IPC hooks, drag/drop FIT decoding, and external-link opening instead of relying on the registered renderer Electron API fallback; direct drag/drop payload and external-link handler tests now use scoped Electron API fixtures instead of registering the fallback candidate; explicit scopes must provide the console through the named provider instead of a direct `consoleRef` property.
- Chart notification suppression migration status: chart prewarm notification suppression now stays in `electron-app/utils/charts/core/renderChartNotificationHelpers.ts` module state instead of `__FFV_suppressNotifications` on the global object.
- Chart loading suppression migration status: chart render loading-state suppression now stays in `electron-app/utils/charts/core/renderChartRuntimeHelpers.ts` module state instead of `__FFV_suppressLoadingState` on the global object, and chart renderer `NODE_ENV` reads plus `process.nextTick` shim access now route through focused process providers in `electron-app/utils/charts/core/renderChartRuntimeHelpersRuntime.ts` instead of returning `globalThis`, using generic `Reflect.get`/`Reflect.set` process probes, directly importing the shared process environment in chart helpers, or exposing a mutable chart runtime environment from the helper module; explicit scopes must provide process access through named providers instead of a direct `chartRuntimeEnvironment` property, and focused helper tests inject process state without redefining `globalThis.process` or `globalThis.window`.
- Chart debug flag migration status: chart debug, verbose chart debug, and fullscreen trace flags now stay in `electron-app/utils/charts/core/chartDebugState.ts` module state and can be toggled through existing renderer dev tools instead of `__FFV_debugCharts`, `__FFV_debugChartsVerbose`, or `__FFV_traceFullscreen` globals.
- Chart theme config provider migration status: chart rendering and hover helpers now resolve theme config through the typed `electron-app/utils/theming/core/theme.ts` import path instead of falling back to `globalThis.getThemeConfig` or `chartHoverGlobal.getThemeConfig`.
- Chart theme runtime migration status: chart theme class, saved-theme, and system-preference reads now route through `electron-app/utils/charts/theming/chartThemeRuntime.ts`; production document, storage, and media-query defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, and explicit runtime scopes stay behind named provider scope functions instead of a broad `globalThis` default scope or direct scope properties.
- Accent color runtime migration status: accent color target lookup and persisted color storage access now route through `electron-app/utils/theming/core/accentColorRuntime.ts` instead of probing `document`, `HTMLElement`, or `localStorage` directly inside `accentColor.ts`; production document, HTMLElement, and localStorage defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers and the shared HTMLElement constructor alias, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct ambient constructor type spellings, or direct document/constructor/storage properties.
- Chart render core runtime migration status: chart render CustomEvent checks, high-resolution timing, date timing, and window availability now route through `electron-app/utils/charts/core/renderChartJSRuntime.ts`; chart performance-monitor start/end timestamps now route through `electron-app/utils/charts/core/renderChartPerformanceMonitorRuntime.ts` instead of calling `performance.now` or `Date.now` directly inside `renderChartPerformanceMonitor.ts`; chart performance-monitor production clock and performance defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, remaining production defaults stay behind named provider scope functions rather than per-property global getters, and explicit runtime scopes must provide CustomEvent, performance, and date-clock primitives through named providers instead of direct browser primitive properties or direct ambient constructor types. RenderChartJS production CustomEvent defaults now also reuse the shared browser runtime provider and constructor alias instead of a local ambient constructor check.
- Chart render renderer-scope migration status: `renderChartJSRuntime.ts` now uses an explicit renderer-scope provider for chart devtools availability instead of resolving `globalThis.window` through the runtime facade, and the production renderer-scope check reuses the shared browser document provider instead of probing `globalThis` directly; explicit runtime scopes must use that provider instead of direct renderer-scope properties.
- Chart plugin-registration migration status: Chart.js plugin registration now tracks registered runtime objects in `electron-app/utils/charts/core/renderChartPluginRegistration.ts` module state instead of writing `__ffvPluginsRegistered` marker properties on the Chart.js constructor.
- Chart listener lifecycle migration status: chart render-request and shared-configuration listener lifecycle state now stays in `electron-app/utils/charts/core/chartListenerState.ts` module state instead of `_fitFileViewerChartListener`, `_fitFileViewerChartListenerAbortController`, `_fitFileViewerSharedConfigurationListener`, or `_fitFileViewerSharedConfigurationAbortController` globals; listener abort-controller creation now routes through `electron-app/utils/charts/core/chartListenerStateRuntime.ts`, production defaults read AbortController through the shared `electron-app/utils/runtime/browserRuntime.ts` provider seam and shared constructor alias, and explicit runtime scopes stay behind a named provider function instead of a broad `globalThis` default scope, local per-runtime global getter, direct ambient constructor type, or direct controller properties.
- Chart request listener runtime migration status: request-render listener registration, fallback container lookup, HTMLElement checks, and CustomEvent checks now route through `electron-app/utils/charts/core/renderChartRequestListenerRuntime.ts`; production event-target, CustomEvent, document, and HTMLElement defaults now reuse shared browser runtime providers and constructor aliases instead of local inline `globalThis` getters or direct ambient constructor types, and explicit runtime scopes must provide their event-target, document, HTMLElement, and CustomEvent providers instead of direct scope properties or falling back to `globalThis`.
- Chart startup runtime migration status: DOMContentLoaded listener registration and renderer-scope detection now route through `electron-app/utils/charts/core/renderChartStartupRuntime.ts`; production listener and document-scope defaults reuse shared browser runtime providers instead of local inline `globalThis` probes, production defaults stay behind named provider scope functions instead of a broad runtime scope or partial `window` scope, and explicit scopes must provide listener registration through the named provider instead of a direct `addEventListener` property.
- Chart DOM helper runtime migration status: no-data message element creation now routes through `electron-app/utils/charts/core/renderChartDomHelpersRuntime.ts` instead of calling `document.createElement` directly inside `renderChartDomHelpers.ts`; production document and HTMLElement defaults now reuse shared browser runtime providers and the HTMLElement constructor type alias instead of local inline `globalThis` getters or direct ambient browser API types, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document/HTMLElement properties.
- Chart settings header runtime migration status: reset-button feedback timing, range-slider chart re-render debounce, settings-header listener abort-controller creation, chart-selection Escape-key document listener registration, settings-panel DOM creation, chart-selection modal DOM creation, export link body append, range-slider style head append, and select change-event construction now route through `electron-app/utils/ui/components/createSettingsHeaderRuntime.ts` instead of calling timer globals, constructing `AbortController`, registering document listeners, touching `document` directly, or calling `new Event("change")` inside `createSettingsHeader.ts`; production AbortController, timer, document, and Event lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, production defaults and explicit test scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct timer, controller, document, document event-target, or Event scope properties, and settings-header JSON export timestamps now reuse `electron-app/utils/logging/loggingTimestampRuntime.ts` instead of constructing ISO timestamps with `new Date()` directly.
- Current chart settings runtime migration status: reset UI timer scheduling, cleanup, and form-control type checks now route through `electron-app/utils/app/initialization/getCurrentSettingsRuntime.ts` instead of calling timer globals or checking ambient input/select constructors directly inside `getCurrentSettings.ts`; production timer and form-control constructor defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers and type aliases, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct timer/form-constructor properties, or direct ambient timer/input/select API types.
- Chart hover effects runtime migration status: chart hover fullscreen resize scheduling, fullscreen target waits, ripple cleanup timers, fullscreen cleanup abort-controller creation, overlay body append/class updates, Escape-key listener registration, and fullscreen-change listener registration now route through `electron-app/utils/charts/plugins/addChartHoverEffectsRuntime.ts` instead of calling animation-frame/timer globals, constructing `AbortController`, mutating `document.body`, or registering document listeners directly; production defaults stay behind named provider scope functions, production document, animation-frame, timer, and AbortController lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, chart hover timer/controller/animation-frame provider contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, and explicit runtime scopes must provide timer, animation-frame, abort-controller, document, and document event-target providers instead of direct properties or falling back to `globalThis`.
- Inline SVG runtime migration status: app icon factory SVG creation, global fullscreen button icons, exit-fullscreen overlay icon, chart hover fullscreen/title icons, chart hover effect runtime SVG creation, quick color switcher icons, keyboard-shortcuts modal icons, settings modal icons, about modal icons, map export/print button icons, loading overlay spinner, add-FIT-to-map control, marker-count selector, data-point filter icons, render-summary controls, map theme toggle, elevation-profile button, map fullscreen control, and map measurement toolbar now route SVG element creation through focused runtime facades instead of calling `document.createElementNS` directly; the icon factory production document default now reuses the shared browser runtime provider instead of a private `getGlobalDocument()` helper or local inline `globalThis` getter, and architecture coverage blocks raw `document.createElementNS` and helper-level document globals from returning anywhere under Electron app source.
- RenderChartJS runtime migration status: chart render session timing, prepared-render timing, chart-data render timing, completion timestamps, chart-action successful-render timestamps, completion event construction, DOM element creation, and devtools window availability now route through `electron-app/utils/charts/core/renderChartJSRuntime.ts` instead of probing `performance.now`, `Date.now`, `globalThis.CustomEvent`, `globalThis.document`, or `globalThis.window` directly inside `renderChartJS.ts` or `renderChartActions.ts`; production clock, document, and performance defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, and explicit runtime scopes must provide date clocks and document access through named providers instead of direct properties or ambient fallbacks.
- Render chart timer runtime migration status: chart render debounce, cache prewarm, timing, and notification-flow timers now route through `electron-app/utils/charts/core/renderChartTimerRuntime.ts` instead of calling timer globals directly inside chart helper modules, render timing debounce clock reads now use that runtime instead of calling `Date.now` directly inside `renderChartTiming.ts`, and notification-flow state timestamps now read through the same runtime clock instead of calling `Date.now` directly inside `renderChartNotificationFlow.ts`; production timer and clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and timer type aliases, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct timer/clock properties.
- Chart devtools registry migration status: development chart helpers now stay in `electron-app/utils/charts/core/chartDevToolsRegistry.ts`, and field-toggle fallback re-render requests use `electron-app/utils/charts/core/chartActionsRegistry.ts` instead of `__chartjs_dev` or `addHoverEffectsToExistingCharts` on the runtime global.
- Field-toggle runtime migration status: field-toggle DOM construction, custom event dispatch, listener abort-controller creation, input checks, and fallback timers now route through `electron-app/utils/ui/components/createFieldTogglesSectionRuntime.ts`; production defaults now reuse shared browser runtime providers and aliases for AbortController, timers, CustomEvent, dispatch, document, and input constructor lookups instead of local inline `globalThis` getters or direct ambient runtime types, and explicit runtime scopes now use named provider functions instead of a broad `globalThis` default scope, direct scope properties, or document-window fallbacks.
- Field-toggle chart-state migration status: field-toggle and bulk-toggle chart re-render requests now resolve the typed chart-state manager through `electron-app/utils/charts/core/chartStateManagerRegistry.ts` instead of importing the concrete chart-state singleton directly; fallback render requests still use the chart-actions registry plus the existing render-request event.
- Vitest Chart.js devtools cleanup migration status: `tests/vitest/setupVitest.mjs` no longer deletes a retired `window.__chartjs_dev` cleanup global; chart devtools lifecycle belongs to `chartDevToolsRegistry`.
- Chart resize test-global migration status: resize listener unit tests now prove registered chart instances are resized without assigning or deleting legacy Chart.js renderer globals, and chart resize listener cleanup now creates abort controllers through `electron-app/utils/app/lifecycle/listenersResizeRuntime.ts` instead of constructing `AbortController` directly in `listenersResize.ts`; resize target access, document reads, DOM constructors, animation frames, timers, and abort-controller creation now stay behind named provider scope functions instead of a broad runtime scope or partial `window` scope, and resize-listener production defaults now reuse shared browser runtime providers for those browser primitives.
- RenderChartJS comprehensive test-global migration status: renderChartJS comprehensive tests now use the typed chart runtime test API instead of deleting retired `Chart`, `ChartZoom`, or `chartjsPluginZoom` globals before imports.
- Chart zoom reset Canvas fixture migration status: chart zoom reset plugin tests now use Vitest-scoped `CanvasRenderingContext2D` stubs when exercising the roundRect polyfill instead of defining or deleting `globalThis.CanvasRenderingContext2D` directly.
- RenderChartJS comprehensive browser fixture migration status: renderChartJS comprehensive tests now install and restore mocked document, window, and browser runtime globals through descriptor-scoped helpers instead of assigning browser fixtures directly onto global objects or deleting globals during cleanup.
- RenderChartJS comprehensive timing fixture migration status: renderChartJS comprehensive performance timing coverage now asserts against a named runtime clock mock instead of reading `global.window.performance.now`.
- RenderChartJS state-API test-global migration status: renderChartJS state API tests no longer install a legacy `window.Chart` fixture for APIs that only exercise state helpers, and their chart-data fixtures now default through `fitFile.rawData` instead of the retired `globalData` path.
- Chart rendered-event active-data migration status: chart-rendered event readiness naming now refers to active FIT chart data instead of renderable global data, and architecture coverage blocks `GlobalData` casing as well as `globalData` in runtime source outside the explicit retired-path quarantine.
- Lap-zone chart test fixture migration status: strict lap-zone chart tests now name their raw-data fixtures as active FIT data instead of `globalData`, matching the production `getActiveFitActivityData()` path.
- Strict chart test environment migration status: strict create-enhanced-chart and zone-chart suites now install and restore browser globals through descriptor-scoped fixtures instead of assigning or deleting `globalThis` properties directly.
- Chart zone color storage fixture migration status: chart zone color utility tests now install and restore the localStorage shim through a descriptor-scoped fixture instead of assigning or deleting `globalThis.localStorage` directly.
- State middleware storage spy migration status: state middleware branch tests now exercise localStorage write failures through a scoped `Storage.prototype.setItem` spy instead of assigning `localStorage.setItem` back during cleanup.
- Chart status indicator test browser fixture migration status: chart status indicator tests now install and restore temporary document, document-defaultView, constructor, timer, and event-listener fixtures through captured descriptors instead of replacing `globalThis.window`, assigning browser globals, assigning event handlers, or deleting globals during cleanup.
- Chart status indicator runtime migration status: chart status DOM lookup, element/text-node creation, body appends, viewport reads, status event listeners, timer scheduling/cleanup, and listener abort-controller creation now route through `electron-app/utils/charts/components/chartStatusIndicatorRuntime.ts`; production defaults stay behind named provider scope functions, default viewport reads use named browser dimensions instead of a generic `Reflect.get(globalThis, key)` helper, production document, HTMLElement, AbortController, event-listener, and timer lookups now reuse shared browser runtime providers and aliases instead of local inline `globalThis` getters or direct ambient runtime types, and explicit runtime scopes must provide document, event-listener, viewport, timer, and constructor providers instead of direct properties or falling back to `globalThis`.
- Chart render-session state-patch migration status: chart render session startup now accepts the lifecycle fallback clear-state update through the named `ChartClearStatePatch` contract instead of casting the patch back to a broad string-indexed record at the session-start boundary.
- Chart canvas runtime migration status: chart canvas element creation now routes through `electron-app/utils/charts/components/createChartCanvasRuntime.ts` instead of calling `document.createElement` directly inside `createChartCanvas.ts`; the production document default now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes must provide the document through that provider instead of direct properties or falling back to `globalThis`.
- Additional theme test browser fixture migration status: additional theme coverage now installs and restores temporary `matchMedia`, `localStorage`, and `getComputedStyle` fixtures through descriptor-scoped helpers instead of reflecting directly onto `globalThis` or deleting globals during cleanup.
- Chart theme listener runtime migration status: chart theme listener event wiring stays behind `electron-app/utils/charts/theming/chartThemeListenerRuntime.ts`; production AbortController, timer, CustomEvent, and document defaults now read through the shared `electron-app/utils/runtime/browserRuntime.ts` provider seam and shared browser-runtime aliases, production defaults and explicit runtime scopes stay behind named provider scope functions instead of a broad `globalThis` default scope, local per-runtime AbortController/timer/document/constructor getters, direct ambient constructor or timer types, or direct scope properties, and explicit runtime scopes must provide timer primitives, browser constructors, and the document body through those providers.
- UI state manager theme test fixture migration status: UI state manager theme coverage now installs and restores temporary `matchMedia` fixtures through a descriptor-scoped helper instead of defining or deleting `globalThis.matchMedia` directly.
- UI state manager runtime migration status: system-theme media-query lookup, theme root/toggle element lookup, document title reads/writes, active-file body state updates, active-file display element lookup and span creation, chart-controls element lookup, drop-overlay element lookup, loading indicator/main-content/progress lookup, sidebar element lookup, measurement-mode element/listener lookup, tab button/content lookup, unload-button element lookup, loading cursor updates, listener abort-controller creation, window resize/beforeunload listener registration, last-notification timestamps, and viewport-state reads now route through `electron-app/utils/state/domain/uiStateManagerRuntime.ts` instead of probing `globalThis.window`, probing `globalThis.matchMedia`, reading or writing `document.title`, toggling `document.body` file-state classes/dataset fields, querying active-file display, chart-controls, drop-overlay, loading-state, sidebar, measurement-mode, tab, theme, or unload-button DOM elements directly, creating active-file display spans directly, checking tab/theme elements against the ambient `HTMLElement` constructor directly, writing `document.body.style.cursor`, registering on `window.addEventListener`, reading window dimensions directly, calling `Date.now` directly, or constructing `AbortController` directly inside `uiStateManager.ts`; production defaults and explicit runtime scopes stay behind named runtime-scope provider functions instead of helper-level document globals, the production AbortController, date-clock, document, HTMLElement, and matchMedia lookups now reuse shared browser runtime providers and aliases instead of local inline `globalThis` or `Date.now` getters or direct ambient type contracts, and explicit runtime scopes must provide event targets, media-query functions, theme root/toggle elements, document title access, active-file body state, active-file display elements and span factories, chart-controls elements, drop-overlay elements, loading-state elements, sidebar elements, measurement-mode elements, tab elements and HTMLElement checks, unload-button elements, body cursor updates, clock reads, viewport state, and AbortController constructors through those providers without falling back to a broad `globalThis`, partial `window` scope, or direct scope properties.
- Credits marquee test runtime fixture migration status: credits marquee tests now pass explicit observer and animation runtimes into `setupCreditsMarquee` instead of stubbing, defining, assigning, or deleting `ResizeObserver`, `requestAnimationFrame`, or `cancelAnimationFrame` globals.
- Credits marquee runtime migration status: resize-listener fallback cleanup, resize event registration, document queries, element checks, observer creation, and animation-frame scheduling now route through `electron-app/utils/ui/layout/enhanceCreditsSectionRuntime.ts` instead of constructing `AbortController` directly in `enhanceCreditsSection.ts` or reading browser globals in the feature module; production defaults now live in an explicit provider object instead of a broad `globalThis` default scope, production controller, document, event-target, HTMLElement, observer, and animation-frame lookups now reuse shared browser runtime providers and aliases instead of local inline `globalThis` getters or direct ambient runtime types, and explicit scopes use named providers instead of direct controller/document/event-target/constructor/frame properties.
- Strict about-modal animation fixture migration status: strict about modal tests now install their immediate `requestAnimationFrame` fixture through a Vitest-scoped global stub instead of assigning, defining, or deleting `globalThis.requestAnimationFrame` directly.
- Strict notification animation fixture migration status: strict notification branch tests now rely on Vitest mock restoration for `window.requestAnimationFrame` instead of assigning the original callback back onto `window` during cleanup.
- Single HR-zone chart test fixture migration status: single HR-zone bar tests now install and restore jsdom browser globals through descriptor-scoped helpers and mock console methods through per-test Vitest spies instead of assigning or deleting browser globals or replacing `global.console` directly.
- Altitude profile chart test fixture migration status: strict altitude profile chart tests now install jsdom browser globals and localStorage through descriptor-scoped helpers and mock console methods through per-test Vitest spies instead of assigning browser globals or a replacement `global.console` directly.
- Speed-vs-distance chart test fixture migration status: strict speed-vs-distance chart tests now install jsdom browser globals and localStorage through descriptor-scoped helpers and mock console methods through per-test Vitest spies instead of assigning browser globals or a replacement `global.console` directly.
- Power-vs-heart-rate chart test fixture migration status: strict power-vs-heart-rate chart tests now install jsdom browser globals and localStorage through descriptor-scoped helpers and mock console methods through per-test Vitest spies instead of assigning browser globals or a replacement `global.console` directly.
- Event-message chart test fixture migration status: strict event-message chart coverage now relies on the jsdom-provided window object instead of assigning `global.window = window` during setup.
- Handle-open-file process fixture migration status: complete file-open tests now install and restore temporary `process.env` coverage through a descriptor-scoped fixture instead of assigning or deleting `globalThis.process` directly.
- Process environment test fixture migration status: runtime process-environment tests now restore the captured `globalThis.process` descriptor instead of deleting the process fixture during cleanup.
- Gyazo OAuth state crypto fixture migration status: OAuth state tests now install and restore the unavailable-crypto fixture through a descriptor-scoped helper instead of deleting `globalThis.crypto` during cleanup.
- Data-point filter async fixture migration status: data-point filter control tests now install and restore temporary animation-frame and microtask fixtures through descriptor-scoped helpers instead of assigning or deleting `globalThis.requestAnimationFrame`, `globalThis.cancelAnimationFrame`, or `globalThis.queueMicrotask` directly.
- Data-point filter runtime migration status: the data-point filter control, element-factory, and panel-controller runtime facades now keep production defaults behind named runtime scopes; data-point filter control production AbortController, document, and microtask lookups, panel-controller production AbortController, document, Node, and animation-frame lookups, plus element-factory production document lookup now reuse shared browser runtime providers instead of local inline `globalThis` getters; data-point filter control and panel-controller AbortController/Node contracts now reuse shared browser-runtime aliases instead of direct ambient constructor types; explicit data-point filter runtimes resolve AbortController, microtask scheduling, animation-frame scheduling/cleanup, document access, viewport reads/listeners, and Node checks from named provider functions instead of broad `globalThis` default scopes, direct scope properties, or document-window fallbacks.
- Shared configuration URLSearchParams fixture migration status: shared configuration tests now install and restore the throwing `URLSearchParams` fixture through a descriptor-scoped helper instead of assigning or deleting `global.URLSearchParams` directly.
- Shared configuration runtime migration status: URL search reads, chart-refresh timer scheduling, and chart-refresh timer cleanup now route through `electron-app/utils/app/initialization/loadSharedConfigurationRuntime.ts` instead of probing location or calling timer globals directly inside `loadSharedConfiguration.ts`; production location and timer defaults now reuse shared browser runtime providers and aliases, and production defaults plus explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct ambient runtime types, or direct location/timer properties.
- System info runtime migration status: system-info DOM lookup now routes through `electron-app/utils/app/initialization/updateSystemInfoRuntime.ts` instead of querying `document` directly inside `updateSystemInfo.ts`; production document lookup now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes must provide document access through that provider instead of direct properties or a broad `globalThis` fallback scope.
- Version info runtime migration status: version-number DOM lookup now routes through `electron-app/utils/app/initialization/loadVersionInfoRuntime.ts` instead of querying `document` directly inside `loadVersionInfo.ts`; Electron API-backed version reads now have an explicit `loadVersionInfoWithOptions` scoped-provider entry point for focused tests instead of requiring the registered renderer Electron API fallback; production document lookup now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, explicit runtime scopes must provide document access through that provider instead of direct properties or a broad `globalThis` fallback scope, and scoped version-info Electron API validation now uses explicit optional-function property checks instead of casting the candidate preload API to a generic record or probing method names with `Reflect.get`.
- Tab-button browser fixture migration status: tab-button behavior tests now install and restore only temporary `getComputedStyle` and `MutationObserver` fixtures through descriptor-scoped helpers instead of assigning, deleting, replacing, or reading through `globalThis.window`, `(global as any).window`, `globalThis.getComputedStyle`, `global.MutationObserver`, or `global.window.MutationObserver` directly.
- Error handling runtime migration status: shared error handling no longer probes `globalRef.performanceMonitor` for ambient telemetry, global error-listener abort-controller creation, browser listener target lookup, `AppError` timestamp creation, and structured error-log ISO timestamps now route through `electron-app/utils/errors/errorHandlingRuntime.ts` instead of keeping a module-level `globalThis` alias, constructing `AbortController` directly, calling `Date.now` directly, or constructing `new Date()` directly inside `errorHandling.ts`; promise-like validation now reads a typed optional `then` property instead of probing arbitrary values through `Reflect.get`; production AbortController, addEventListener, and date-clock defaults now read through shared `electron-app/utils/runtime/browserRuntime.ts` providers, the AbortController contract now reuses the shared browser-runtime alias instead of a direct ambient constructor type, production listener binding stays inside the shared browser-runtime provider instead of a local error-handling `globalThis` bind, production defaults stay behind named provider scope functions and an error-listener-specific target accessor instead of generic `Reflect.get(globalThis, ...)` probes, local per-runtime browser getters, local `Date.now` getters, generic global-event-target bridges, or direct runtime properties; explicit runtime scopes must use those providers instead of direct `AbortController`, listener, clock, date-constructor, or event-target properties; and its tests no longer install or delete temporary performance-monitor or browser-listener globals.
- Renderer error-message migration status: renderer startup error notifications now extract optional `message` values through a focused `RendererErrorLike` shape instead of casting arbitrary thrown values to a broad string-indexed record.
- Shared performance monitor runtime migration status: shared operation timing now routes through `electron-app/utils/performance/performanceMonitorRuntime.ts` instead of reading `performance.now` directly inside `performanceMonitor.ts`; the production performance default now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes must provide performance access through named providers instead of direct `performance` properties or broad `globalThis` fallback scopes.
- Chart notification caller migration status: lap-zone chart renderers, single-zone chart renderers, and the zone-color picker reset flow now import the typed notification helper directly instead of probing `showNotification` on renderer globals.
- Zone-color modal helper migration status: the zone-color picker now imports inline selector refresh, color-data clearing, and full settings reset helpers directly instead of probing `updateInlineZoneColorSelectors`, `clearZoneColorData`, or `resetAllSettings` on renderer globals.
- Inline zone color selector runtime migration status: inline zone color selector DOM construction, custom event dispatch, listener abort-controller creation, input/select checks, and delayed updates now route through `electron-app/utils/ui/controls/createInlineZoneColorSelectorRuntime.ts`; production defaults now reuse shared browser runtime providers for AbortController, CustomEvent, dispatch, document, element constructors, and timers, inline selector controller/event/dispatch/element/timer contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, and explicit runtime scopes now use named provider functions instead of a broad `globalThis` default scope, direct scope properties, document-window fallbacks, or local inline `globalThis` getters.
- Inline zone color selector chart-state migration status: zone scheme changes, zone color resets, and individual zone color changes now resolve the typed chart-state manager through `electron-app/utils/charts/core/chartStateManagerRegistry.ts` instead of importing the concrete chart-state singleton directly; fallback rendering still uses the direct renderer import with a render-request event fallback.
- Accent color picker runtime migration status: modal lookup/body append, element and text-node creation, style element creation/head append, focus tracking, preview DOM queries, element-list queries, element constructor checks, modal listener abort-controller creation, and Escape-key document listener registration now route through `electron-app/ui/modals/accentColorPickerRuntime.ts` instead of using ambient document open/style/focus/preview/construction access, constructing `AbortController`, or registering document listeners directly inside `accentColorPicker.ts`; production document, element-constructor, and controller lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, controller and element-constructor contracts now reuse shared browser-runtime aliases instead of direct ambient constructor type spellings, and explicit runtime scopes must use named providers instead of direct properties or broad global defaults.
- Zone-color picker test-global migration status: zone-color picker unit tests now assert inline selector, chart render, and color-data helper globals stay absent without deleting or setting `updateInlineZoneColorSelectors`, `renderChartJS`, or `clearZoneColorData` on `globalThis`.
- App menu export migration status: `createAppMenu` and the app-menu barrel now use named source exports/imports instead of source-level `module.exports` or `require("./createAppMenu.js")`; the old `__FFV_createAppMenuExports` global bridge stays removed, and its Electron menu tests now use module-local fixture state plus native ESM imports instead of `__electron*`, call-log globals on `globalThis`, or `createRequire`.
- Retired renderer compatibility test-global status: ordinary unit tests no longer create or read `__FFV_suppressNotifications`, `__FFV_suppressLoadingState`, or `__FFV_createAppMenuExports`; the renderChartJS comprehensive test uses ESM mocks instead of a `utils.require` module-cache bridge; architecture coverage keeps those retired global names and module-cache hooks out of tests except for the boundary regex definitions.
- App menu Electron access migration status: `createAppMenu` now resolves Electron only through `electron-app/main/runtime/electronAccess.ts` instead of reading the test-only `__electronHoistedMock` bridge directly, and its recent-file/file-access dependencies are native imports instead of source-level `require(...)` calls.
- FIT file state manager migration status: file-open and render flows now import the typed `fitFileStateManager` singleton directly instead of resolving `__FFV_fitFileStateManager` from the global object, and load-completion, loading-state, and metrics timestamps now route through `electron-app/utils/state/domain/fitFileStateRuntime.ts` instead of calling `Date.now` directly inside `fitFileState.ts`; production date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`.
- Main-process state manager export migration status: `electron-app/utils/state/integration/index.ts` now resolves `mainProcessStateManager.ts` through its CommonJS module exports instead of the `__FFV_mainProcessStateManagerExports` global bridge.
- State manager API migration status: `masterStateManager.ts` now resolves state-manager test doubles only through module-local test overrides and production typed imports instead of the `__STATE_MANAGER_API__` or `__FFV_MOCKS__` global bridges, CJS module-cache probing, or `node:module` fallback loading.
- State manager defaults runtime migration status: `stateManagerDefaults.ts` now resolves startup timestamps and default document titles through `stateManagerDefaultsRuntime.ts` instead of probing `globalThis.performance`, `Date.now`, `typeof document`, or `document.title` directly; production document, performance, and date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, and explicit runtime scopes must provide document/performance/date-clock access through named providers instead of direct properties or ambient fallbacks.
- Core state manager runtime migration status: state-history timestamps now resolve through `stateManagerRuntime.ts` instead of calling `Date.now` directly inside `stateManager.ts`; production date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, and explicit timing scopes stay behind a named provider function instead of direct scope properties.
- Unified state manager runtime migration status: unified facade snapshot timestamps now resolve through `unifiedStateManagerRuntime.ts` instead of calling `Date.now` directly inside `unifiedStateManager.ts`; production date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, and explicit timing scopes stay behind a named provider function instead of direct scope properties.
- State storage runtime migration status: core state persistence, persistence middleware, master-state UI theme bootstrap, and settings helper chart-key cleanup now route `fitFileViewer_state`, `ffv_state_*`, `ffv-theme`, `fitFileViewer_theme`, and `chartjs_*` storage reads/writes/removals through `electron-app/utils/state/core/stateStorageRuntime.ts` instead of touching `localStorage` directly inside `stateManager.ts`, `stateMiddleware.ts`, `masterStateManager.ts`, or `settingsStateHelpers.ts`; production storage defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, explicit runtime scopes stay behind a named storage provider, and architecture coverage rejects ambient storage getter fallbacks or direct scope properties.
- State middleware performance history migration status: performance middleware now stores its rolling history in module-owned state exposed through typed history helpers instead of `globalThis._statePerformance`; branch and comprehensive state middleware tests now seed and read that history explicitly through the helper API. Middleware handler duration timing, performance-middleware duration timing, and performance-history timestamp reads now route through `electron-app/utils/state/core/stateMiddlewareRuntime.ts` instead of calling `performance.now` or `Date.now` directly inside `stateMiddleware.ts`; production performance and date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`.
- Generic storage utility migration status: safe Web Storage helpers now resolve ambient `localStorage` through a named default provider instead of casting `globalThis` inside `resolveStorage`, and that provider now reuses `electron-app/utils/runtime/browserRuntime.ts` instead of owning an explicit `globalThis.localStorage` read or generic `Reflect.get(globalThis, "localStorage")` probe.
- Computed state manager runtime migration status: auto-theme dark-scheme checks, computation duration timing, `lastComputed` timestamps, and uptime calculations now resolve through `computedStateManagerRuntime.ts` instead of probing `globalThis.matchMedia`, `performance.now`, or `Date.now` directly inside `computedStateManager.ts`; production media-query, performance, and date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, the media-query provider type now uses the shared browser-runtime alias instead of a direct ambient browser API type, and explicit media-query/timing scopes stay behind named provider functions instead of direct scope properties or ambient fallbacks.
- Master state manager runtime migration status: `masterStateManager.ts` now routes development-scope location/event-target/document-element checks, global error listeners, global event dispatch, theme-change event construction, window lifecycle listeners, document drag/drop and keyboard listener registration, drag-over body class updates, loading-sensitive element lookup, listener abort-controller creation, component initialization timestamps, startup-time state writes, global-error timestamps, promise-rejection timestamps, and performance-monitor timestamps through `masterStateRuntime.ts` instead of probing `globalThis.window`, `globalThis.location`, `globalThis.addEventListener`, `globalThis.dispatchEvent`, registering on `window.addEventListener` or `document.addEventListener`, querying `document` directly, reading `document.documentElement`, touching `document.body`, constructing `AbortController`, constructing `CustomEvent`, or calling `Date.now` directly; production defaults and explicit browser/development/timing scopes stay behind named provider functions instead of a broad `globalThis` default scope, helper-level document globals, or direct scope properties, the production AbortController, listener, interval, CustomEvent, date-clock, dispatch, document, event-target, location, and performance lookups now reuse shared browser runtime providers and aliases instead of local inline `globalThis` or `Date.now` getters or direct ambient type contracts, the default runtime no longer reads ambient `globalThis.__DEVELOPMENT__`, and window/document lifecycle registration, document-element checks, body class updates, loading-sensitive lookup, theme-change event construction, plus clock reads now use explicit event-target/body/element/query/CustomEvent/date providers instead of partial ambient providers.
- Master state performance monitor runtime migration status: performance-monitor interval scheduling/cleanup and memory reads now route through `masterStateRuntime.ts` instead of calling `setInterval`, `clearInterval`, or reading `performance.memory` directly inside `masterStateManager.ts`; production defaults stay behind named interval and performance-memory providers backed by shared browser runtime lookups, and explicit runtime scopes must provide provider functions instead of direct interval or performance-memory properties.
- State integration runtime migration status: state integration performance-monitor intervals, persistence timeout scheduling, UI-state storage reads, date-clock reads, and performance-memory reads route through `stateIntegrationRuntime.ts`; production timer, storage, clock, and performance defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and timer/interval type aliases, and explicit runtime scopes must provide named provider functions instead of direct timer, storage, clock, or performance properties.
- State manager singleton subscription migration status: duplicate-subscription cleanup now stays in `electron-app/utils/state/core/stateManager.ts` module state instead of `__ffvSingletonStateSubscriptions` on the global object.
- Master state performance monitoring migration status: state-change counting now uses typed state-manager subscription cleanup instead of monkey-patching `__state_debug.setState`.
- Master state manager test browser fixture migration status: master state manager comprehensive tests now install and restore document, window, location, storage, performance, timer, and listener globals through descriptor-scoped helpers instead of deleting globals during cleanup.
- State devtools debug-console migration status: `stateDevTools.ts` now exposes debugging and performance helpers only through typed module exports instead of publishing `window.__stateDebug`.
- State devtools runtime migration status: `stateDevTools.ts` now checks development-scope availability through `stateDevToolsRuntime.ts` instead of probing `globalThis.window` or `globalThis.location` directly; production defaults stay behind named provider scope functions instead of a broad `globalThis` default scope, and explicit runtime scopes must use the location provider instead of direct location properties.
- State devtools renderer-scope migration status: `stateDevToolsRuntime.ts` now uses an explicit renderer-scope provider for development gating instead of resolving `globalThis.window` through the runtime facade, and the production renderer-scope check reuses the shared browser document provider instead of probing `globalThis` directly; explicit runtime scopes must use that provider instead of direct renderer-scope properties.
- State devtools interval/runtime migration status: `stateDevTools.ts` now routes performance-monitor interval scheduling/clearing, debug timestamps, performance durations, and browser memory reads through `stateDevToolsRuntime.ts` instead of calling interval globals, `Date.now`, `performance.now`, or `performance.memory` directly; explicit runtime scopes must now provide interval, date-clock, location, and performance primitives through named providers instead of falling back to `globalThis` or direct timer/clock/location/performance properties, and production timer, clock, location, and performance defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and interval type aliases instead of local inline browser-global getters.
- State devtools test-global migration status: state devtools tests now assert the typed debug utilities stay off `globalThis.__stateDebug` without deleting that retired debug global before cleanup or initialization.
- State integration development debug migration status: state integration startup no longer publishes `__state_debug`, and the core state manager no longer exports the mutable root state through `__debugState`; development diagnostics use typed module APIs instead of renderer globals.
- Chart tab integration migration status: chart-tab lifecycle coordination now stays on the typed `chartTabIntegration` module singleton instead of publishing `window.chartTabIntegration`, chart-state access now routes through `chartStateManagerBootstrap.ts` and `chartStateManagerRegistry.ts` instead of importing the concrete chart-state singleton directly inside the integration module, app cleanup now calls `destroy()` directly instead of a compatibility `cleanup()` alias, chart-tab DOM queries now reuse shared browser runtime providers and constructor aliases for production document and HTMLElement defaults, and explicit runtime scopes no longer borrow ambient `document` or `globalThis.HTMLElement`, spell direct ambient constructor types, or accept direct runtime scope properties.
- Chart tab integration test-global migration status: chart-tab integration unit tests now assert the typed singleton stays off `globalThis` without deleting `chartTabIntegration` before each case.
- Chart state manager migration status: chart render request routing, chart updater status/update/theme-change paths, chart theme listener handoff, app cleanup, shared-configuration refreshes, and chart-settings rerender requests now resolve the typed chart-state manager through `electron-app/utils/charts/core/chartStateManagerRegistry.ts` instead of publishing or reading `window.chartStateManager` or importing the concrete chart-state singleton from update entrypoints, app cleanup now calls the registered `destroy()` lifecycle hook instead of a compatibility `cleanup()` alias, chart-state-manager timer, clock, document, and HTMLElement defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, DOM runtime defaults and timer/HTMLElement provider types stay behind named provider scope functions and shared browser-runtime aliases instead of direct runtime scope properties, a broad `globalThis` default scope, or direct ambient browser API types, successful render timestamp writes now read through `chartStateManagerRuntime.ts` instead of calling `Date.now` directly inside `chartStateManager.ts`, chart updater status timestamps now read through `chartUpdaterRuntime.ts` instead of constructing `new Date()` directly inside `chartUpdater.ts`, and global chart status log timestamps now read through `globalChartStatusLogRuntime.ts` instead of constructing `new Date()` directly inside `createGlobalChartStatusIndicator.ts`.
- Chart actions migration status: chart render lifecycle helpers now resolve chart actions through `electron-app/utils/charts/core/chartActionsRegistry.ts` instead of publishing or reading `window.chartActions`, and the lifecycle accessor is named for registered chart actions instead of the retired global action bridge.
- Chart runtime helper test-global migration status: render chart runtime helper tests now prove registry-based chart actions and state-manager resolution without creating or deleting retired `chartActions` or `chartStateManager` globals, and they restore process/window fixtures through captured descriptors instead of deleting those globals during cleanup.
- Direct chart rerender runtime migration status: direct chart rerender container lookups now route through `electron-app/utils/charts/core/renderChartDirectRerenderRuntime.ts` with shared browser runtime document and HTMLElement providers plus the shared HTMLElement constructor alias for production defaults instead of local inline `globalThis` getters, direct ambient constructor types, a broad `globalThis` default scope, or legacy direct runtime scope properties.
- Chart UI state manager migration status: chart controls visibility now relies on the existing `charts.controlsVisible` state subscription instead of reading `window.uiStateManager` for panel visibility updates.
- File-access policy migration status: approved FIT-file paths now stay in `electron-app/main/security/fileAccessPolicyState.ts` module state instead of `__ffvFileAccessPolicyState` on the global object.
- Tab-button enabled-state migration status: private tab-button enabled bookkeeping, mutation observer state, and diagnostic helpers now stay in `electron-app/utils/ui/controls/enableTabButtons.ts` module exports/state instead of `tabButtonsCurrentlyEnabled`, `tabButtonObserver`, or tab-button helper functions on the global object. Tab-button production timer, MutationObserver, and renderer-document defaults now reuse shared browser runtime providers and stay behind named runtime scopes; explicit tab-button runtime scopes must provide timer primitives, mutation observer constructors, and renderer-scope detection through named providers, helper production defaults now reuse shared browser runtime providers for document, computed-style, HTMLElement constructor, and renderer-scope detection, helper runtime scopes must provide document, computed-style, and HTMLElement constructor access through named providers plus renderer-scope detection, and debug production timer, computed-style, renderer-document, and AbortController defaults now reuse shared browser runtime providers. Explicit debug runtime scopes must provide timers, computed-style access, renderer-scope detection, and AbortController access through named providers, instead of falling back to a broad runtime scope, direct runtime/helper/debug properties, partial `window` scope, or the retired compatibility MutationObserver hook.
- Tab-button test-global migration status: tab-button behavior tests now assert the retired enabled-state flag, observer, and diagnostic helpers stay absent without deleting `tabButtonsCurrentlyEnabled`, `tabButtonObserver`, or tab-button helper names from global scopes.
- Vitest tab-button cleanup migration status: `tests/vitest/setupVitest.mjs` no longer disconnects or deletes a retired `window.tabButtonObserver` cleanup hook; tab-button observer lifecycle belongs to the typed tab-button module state.
- Tab-state manager migration status: the tab-state manager singleton now stays behind `electron-app/utils/ui/tabs/tabStateManager.ts` module exports instead of `tabStateManager` on the global object, and tab test fallbacks now use `electron-app/utils/ui/tabs/tabTestEnvironment.ts` instead of `__vitest_effective_document__` or `__vitest_effective_stateManager__` globals; `tests/vitest/setupVitest.mjs` no longer publishes the retired effective-document global.
- Tab-state document runtime migration status: tab-state manager document helpers and click-target element checks now resolve documents and DOM constructors through `electron-app/utils/ui/tabs/tabDocumentRuntime.ts` instead of probing `globalThis.document` inside each helper module or checking ambient `Element`/`HTMLElement` constructors inside `tabStateManager.ts`; production document and DOM-constructor defaults now reuse shared browser runtime providers and aliases instead of local inline `globalThis` getters, direct ambient runtime types, or a broad `globalThis` default scope, and explicit runtime scopes must now provide documents and DOM constructors through named providers instead of direct `document`, `Element`, or `HTMLElement` properties.
- Tab-state map invalidation runtime migration status: map-tab invalidation scheduling and cancellation plus Zwift iframe/status DOM construction and load-listener cleanup now route through `electron-app/utils/ui/tabs/tabStateManagerHandlersRuntime.ts` instead of calling timing globals, constructing `AbortController`, or creating Zwift DOM nodes directly in `tabStateManagerHandlers.ts`; production defaults now reuse shared browser runtime providers and browser-runtime type aliases for timers, animation frames, document lookup, and AbortController construction, explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct scope properties, or direct ambient constructor/timer/frame types, and explicit scopes must provide document, fallback timer, animation-frame, and controller primitives through providers instead of falling back to `globalThis`.
- Active-tab document runtime migration status: active-tab updates now resolve tab documents and keyboard-event checks through `electron-app/utils/ui/tabs/updateActiveTabRuntime.ts` instead of probing `globalThis.document`, `globalThis.window`, or ambient `KeyboardEvent` inside the feature module; production document and KeyboardEvent lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, and production defaults plus explicit runtime scopes stay behind named provider functions instead of a broad runtime scope, direct `document` or `KeyboardEvent` properties, or partial `window` scope.
- Active-tab fallback fixture migration status: active-tab fallback tests now install and restore only missing-document fixtures through descriptor-scoped helpers and use the tab-test document override for alternate DOM coverage instead of replacing `globalThis.window` with a `window.document` fallback or deleting those globals during cleanup.
- Tab-state manager test-global migration status: tab-state manager behavior tests now prove summary rendering uses typed imports without deleting a retired `window.renderSummary` global first.
- Main UI drag/drop handler migration status: the renderer startup drag/drop handler now stays behind the `mainUiDragDropHandler` module export in `electron-app/main-ui.ts` instead of `dragDropHandler` on the global object, and drag/drop FIT decoder candidate typing now uses a named readonly alias backed by the shared file API-domain contract instead of deriving from the monolithic `ElectronAPI` type or carrying a generic local interface.
- Drag/drop runtime migration status: drag-over overlay scheduling, cancellation, file-reader construction/listener cleanup, global drag/drop event-target resolution, drag-target document checks, and dropped-file operation-id clock reads now route through `electron-app/utils/ui/dragDropHandlerRuntime.ts` instead of calling animation-frame globals, binding listeners to `globalThis`, reading `document` directly, constructing `FileReader`, constructing `AbortController`, or calling `Date.now` directly in `dragDropHandler.ts`; production defaults now reuse shared browser runtime providers and browser-runtime type aliases for abort controllers, animation frames, clock reads, document lookup, event-target lookup, and FileReader construction, and explicit test scopes stay behind named provider functions instead of direct controller, file-reader, document, event-target, animation-frame, or clock scope properties.
- Main UI development helper migration status: menu-injection and development-cleanup helpers now stay behind the `requestMainUiMenuInjection` and `runMainUiDevelopmentCleanup` module exports in `electron-app/main-ui.ts` instead of `injectMenu` or `devCleanup` on the global object.
- Main UI startup test-global migration status: main UI startup and strict-flow tests now assert retired renderer globals stay absent without deleting `devCleanup`, `injectMenu`, `showFitData`, `renderChartJS`, or `cleanupEventListeners` from `globalThis`.
- Main-process development helper migration status: `electron-app/main/dev/exposeDevHelpers.ts` now returns development helper functions through its module export instead of publishing `devHelpers` on `globalThis`.
- Main-process development helper test-global migration status: main-process unit tests now assert dev helpers stay off `globalThis` without deleting the retired `devHelpers` global during cleanup.
- Show FIT data map-render state migration status: initial map rendering now reads and writes `map.isRendered` through the state manager instead of tracking `isMapRendered` on the global object.
- Chart controls state migration status: state startup no longer reads, rewrites, or migrates `chartControlsState` globals, and the deprecated no-op `migrateChartControlsState` export has been removed; chart controls visibility must use the explicit `charts.controlsVisible` state path.
- Renderer state startup migration status: renderer state startup now initializes the state manager and mirrors `app.isOpeningFile` directly through a local ref instead of creating a legacy appState proxy.
- Renderer state startup dependency migration status: renderer state startup now owns the named `RendererStateStartupCoreModules` `masterStateManager`/`subscribeAppDomainPath` dependency contract locally, validates the state manager shape locally, and no longer accepts the broad module-record coercion helper from the renderer entrypoint or derives the contract from `RendererCoreModules`.
- Renderer import-time bootstrap migration status: import-time startup no longer resolves legacy appState test overrides just to touch `app.startTime`; startup state coverage now goes through the typed app-domain facade exposed by core module resolution.
- Renderer import-time function-bridge migration status: import-time theme setup, listener setup, app-domain state touch, and app-domain subscription touch now call typed core-module functions directly, and the shared renderer `callUnknownFunction` bridge has been removed from `coreModuleResolution.ts`.
- Renderer import-time dependency migration status: import-time bootstrap no longer accepts the renderer entrypoint's generic `toModuleRecord` helper; master-state-manager test override normalization now uses local named override-module and initializable-manager shapes instead of a string-indexed record helper, and production import-time setup owns the typed `RendererImportTimeCoreModules` contract needed for theme setup, listener setup, app-domain state touches, and notifications instead of accepting or deriving from the whole `RendererCoreModules` object.
- Renderer application startup runtime migration status: startup listener abort-controller creation, delayed production update-check scheduling, and timer cancellation now route through `electron-app/renderer/applicationStartupRuntime.ts` instead of calling browser primitives directly in `applicationStartup.ts`; renderer startup performance start/end timing now routes through `electron-app/renderer/startupPerformanceMonitorRuntime.ts` instead of calling `performance.now` directly in `startupPerformanceMonitor.ts`; production startup defaults now read browser controller, timer, and performance primitives through shared `electron-app/utils/runtime/browserRuntime.ts` providers and browser-runtime type aliases instead of capturing `globalThis` inside startup runtimes, and explicit runtime scopes must now provide controller/timer/clock primitives through named providers instead of direct properties or falling back to `globalThis` or direct `performance` properties.
- Renderer logging timestamp migration status: `logWithLevel.ts` and `rendererLogger.ts` now build ISO log prefixes through `electron-app/utils/logging/loggingTimestampRuntime.ts` instead of constructing `new Date()` directly in logging call sites; production defaults stay behind a named date-constructor provider, and explicit runtime scopes must provide dates through `getDateConstructor` instead of direct `Date` properties.
- Renderer application startup function-bridge migration status: application startup now invokes resolved `setupTheme` and `setupListeners` functions directly from typed core modules instead of routing theme/listener setup through the generic renderer `callUnknownFunction` bridge.
- Renderer application startup dependency migration status: application startup now owns a local `RendererApplicationStartupCoreModules` dependency contract for startup actions, theme setup, listener setup, app-domain start-time touch, and notifications instead of typing or deriving the startup flow from the whole `RendererCoreModules` object.
- Renderer environment runtime migration status: development-mode and environment-name defaults now resolve focused `RendererEnvironmentInput` values through `electron-app/utils/app/initialization/rendererEnvironmentRuntime.ts` instead of binding `globalThis` directly in `rendererEnvironment.ts`, `getEnvironment`, or `isDevelopmentMode` default parameters; the retired broad `getGlobalScope` handoff is replaced by named providers for the development flag, document, registered Electron API candidate, and location; production document and location defaults now reuse shared browser runtime providers instead of local inline `globalThis` getters, while development-flag and Electron API candidate defaults use explicit named global readers instead of generic `Reflect.get(globalThis, ...)` probes; renderer startup runtime environment capture now uses a named renderer-scope provider instead of resolving `globalThis.window`; main-UI external-link and unload flows now resolve default documents through `electron-app/renderer/mainUiRuntimeEnvironment.ts` instead of defaulting parameters to `document`; explicit callers must now pass focused renderer environment inputs and main-UI document references through named providers instead of direct global-scope properties.
- Renderer environment API naming migration status: renderer environment TSDoc and focused unit coverage now describe the `RendererEnvironmentInput` contract directly instead of referring to a broad global-scope object, with architecture coverage blocking the stale `@param globalScope` and `Global-like object to inspect` wording from returning.
- Renderer application lifecycle runtime migration status: DOMContentLoaded and beforeunload lifecycle listener cleanup now creates abort controllers through `electron-app/renderer/applicationLifecycleWiringRuntime.ts` instead of constructing `AbortController` directly in `applicationLifecycleWiring.ts`; the beforeunload listener now accepts the explicit `rendererEventTarget` handoff from renderer startup instead of a `windowTarget` option or retired `globalEventTarget` lifecycle property; production defaults now read AbortController through the shared `electron-app/utils/runtime/browserRuntime.ts` provider rather than a local lifecycle-runtime `globalThis` getter, the runtime contract now reuses the shared browser-runtime AbortController alias instead of a direct ambient constructor type, and explicit runtime scopes must now provide AbortController through a named provider instead of direct properties.
- Renderer file-input startup runtime migration status: delegated and import-time file-input listener cleanup now creates abort controllers through `electron-app/renderer/fileInputStartupRuntime.ts` instead of constructing `AbortController` directly in `fileInputStartup.ts`; delegated file-input target checks now resolve the `HTMLInputElement` constructor through the same runtime facade instead of reading the ambient constructor or accepting an option-level constructor escape hatch; beforeunload cleanup now accepts the explicit `rendererEventTarget` handoff through `fileInputStartup.ts` and `fileInputWiring.ts` instead of a `windowTarget` parameter or retired `globalEventTarget` file-input property; production defaults now read AbortController and HTMLInputElement through shared `electron-app/utils/runtime/browserRuntime.ts` providers rather than local file-input runtime `globalThis` getters, runtime contracts now reuse shared browser-runtime aliases instead of direct ambient constructor types, and explicit runtime scopes must now provide constructors through named providers instead of direct properties.
- Renderer file-input function-bridge migration status: immediate, import-time, delegated, and test-override file-input handlers now call an explicit `RendererFileOpenHandler` directly instead of routing selected files through the generic renderer `callUnknownFunction` bridge.
- Renderer lifecycle listener contract migration status: core module resolution now resolves `setupListeners` through the lifecycle `SetupListenersOptions` contract, renderer startup/import-time setup builds that contract through a typed adapter, and selected-file change handling stays owned by dedicated file-input wiring instead of a duplicate application-startup listener.
- Renderer setup-theme contract migration status: core module resolution now resolves `setupTheme` through a named renderer setup-theme contract, so renderer startup and import-time theme setup no longer carry that slot through the generic unknown renderer function bridge.
- Renderer debug function bridge migration status: `UnknownRendererFunction` is no longer exported from renderer core-module resolution; development debug tooling now keeps its permissive debug-call helper local instead of making it part of the core-module contract surface.
- Renderer test-only bootstrap runtime migration status: test-only DOMContentLoaded and window-load listener cleanup now creates abort controllers through `electron-app/renderer/testOnlyBootstrapRuntime.ts` instead of constructing `AbortController` directly in `testOnlyBootstrap.ts`; the window-load listener now accepts the explicit `rendererEventTarget` handoff from renderer startup instead of a `windowTarget` option or retired `globalEventTarget` test-only bootstrap property; production defaults now read AbortController through the shared `electron-app/utils/runtime/browserRuntime.ts` provider rather than a local test-only runtime `globalThis` getter, the runtime contract now reuses the shared browser-runtime AbortController alias instead of a direct ambient constructor type, and explicit runtime scopes must now provide AbortController through a named provider instead of direct properties.
- Renderer test-only function-bridge migration status: test-only DOMContentLoaded listener and window-load theme setup now resolve narrow setup-listener/setup-theme function contracts and call them directly instead of routing mocked startup modules through the generic renderer `callUnknownFunction` bridge.
- Renderer test-only override migration status: test-only bootstrap now reads only the named `setupListeners`, `setupTheme`, `applyTheme`, and `listenForThemeChange` override exports it needs instead of normalizing mocked modules through a reusable `toModuleRecord` helper.
- Network utility runtime migration status: fetch, AbortController creation, and timeout scheduling/clearing now route through `electron-app/utils/net/networkUtilsRuntime.ts` instead of calling network or timer globals directly in `networkUtils.ts`; production fetch, controller, and timer defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and browser-runtime type aliases instead of owning local per-runtime browser-global getters, production defaults stay behind named provider scope functions instead of a broad `globalThis` default scope, and explicit runtime scopes must now provide fetch/controller/timer primitives through named providers instead of direct properties or falling back to `globalThis`.
- Renderer app-domain state facade migration status: renderer core module resolution now imports `electron-app/utils/state/domain/appDomainState.ts` for app-domain reads and subscriptions instead of importing the legacy appState manager directly, and app-domain subscription event timestamps now route through `electron-app/utils/state/domain/appDomainStateRuntime.ts` instead of calling `Date.now` directly inside `appDomainState.ts`; production date-clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, and explicit runtime scopes stay behind named provider functions instead of direct date properties.
- Renderer app-domain contract migration status: app-domain facade exports now include typed getter/subscriber contracts, and core module resolution uses those contracts for `getAppDomainState`, `subscribeAppDomain`, and `subscribeAppDomainPath` instead of generic unknown renderer function slots.
- Renderer core module test override migration status: `electron-app/renderer/coreModuleResolution.ts` now resolves focused startup test doubles through module-local override state instead of reading the shared `__vitest_manual_mocks__` global registry, and renderer startup callers now use test-override resolver names instead of retired manual-mock terminology.
- Renderer core-module resolution helper migration status: `toModuleRecord` is no longer exported from `coreModuleResolution.ts`; broad module-record coercion remains private to the dynamic import/test override boundary instead of being a reusable renderer startup helper.
- Renderer core-module contract migration status: the broad resolved core-module aggregate is now private to `coreModuleResolution.ts`; renderer startup modules own local dependency contracts instead of importing a reusable `RendererCoreModules` facade, and the private resolver shape still exposes only named startup dependencies instead of a string index signature.
- Export utility test override migration status: `electron-app/utils/files/export/exportUtils.ts` now resolves focused notification and theme test doubles through the narrow `__setTestDeps` dependency override instead of reading the shared `__vitest_manual_mocks__` global registry or exposing a map-style manual-mock override API.
- Export utility window fixture migration status: root and colocated export utility tests no longer install stale `globalThis.window` fixtures; chart-export DOM coverage uses explicit JSDOM document/constructor fixtures, and colocated export coverage relies on the runtime/document fixtures it actually exercises.
- Export utility runtime migration status: print image-load cleanup, Gyazo modal cleanup, Imgur modal cleanup, export modal Escape-key listener registration, export modal previous-focus lookup, download-link body appends, and export modal/overlay body appends now route through `electron-app/utils/files/export/exportUtilsRuntime.ts` instead of constructing `AbortController`, registering document listeners directly, checking `document.activeElement instanceof HTMLElement`, or appending export elements through `document.body` inside `exportUtils.ts`; destructive-action confirmation prompts and print-window opening also route through explicit runtime function providers instead of probing `globalThis.window`, injecting a partial `window`, or calling `window.open` directly inside `exportUtils.ts`; export storage, secure-random, document event-target, and HTMLElement defaults now use named provider functions instead of direct `globalThis.localStorage`, whole-global crypto access, repeated `globalThis.document`/`globalThis.HTMLElement` reads, or helper-level document globals; production defaults and explicit export runtime scopes stay behind named provider scope functions rather than a broad `globalThis` default scope or direct scope properties, the production AbortController, confirm, crypto, document, HTMLElement, opener, and storage lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, export runtime AbortController and HTMLElement provider contracts now reuse shared browser-runtime constructor aliases instead of direct ambient constructor type spellings, default browser helpers bind confirm/open calls to `globalThis` without a module-level `browserGlobal` alias or ad hoc optional browser-global fields, and chart export metadata/date filename generation now reuses `electron-app/utils/logging/loggingTimestampRuntime.ts` instead of constructing ISO timestamps with `new Date()` directly.
- Export utility OAuth Electron API migration status: `authenticateWithGyazo` now accepts an explicit renderer Electron API scope for tests that need the Gyazo server bridge, so OAuth state coverage no longer registers a shared renderer fallback candidate; scoped export Electron API validation now uses explicit optional-function property guards instead of casting the candidate preload API to a generic record.
- Export utility Electron API contract migration status: export utility preload candidates now compose the shared clipboard and Gyazo API-domain contracts instead of deriving a local helper shape from the monolithic `ElectronAPI` type.
- Export utility root test Electron API migration status: root export utility authentication tests now pass an explicit renderer Electron API scope into `authenticateWithGyazo` instead of registering a shared renderer fallback candidate for the whole test file.
- Export utility colocated test Electron API migration status: colocated export utility authentication tests now pass an explicit renderer Electron API scope into `authenticateWithGyazo` instead of registering a shared renderer fallback candidate for the whole test file.
- Export utility Gyazo modal Electron API migration status: `createGyazoAuthModal` now accepts an explicit renderer Electron API scope for server-mode cleanup tests, so Gyazo modal coverage no longer registers a shared renderer fallback candidate.
- Gyazo OAuth callback page migration status: the local success response no longer emits an inline `window.close()` script; the callback server now returns a static close-note page and tests/boundary guards prevent reintroducing script-driven browser globals in that response.
- Export utility chart clipboard Electron API migration status: PNG clipboard export helpers now accept an explicit renderer Electron API scope through chart and combined-chart copy paths, so chart export coverage no longer registers a shared renderer fallback candidate for Electron clipboard bridge cases; Imgur upload response parsing now uses object and property guards instead of casting response payloads to generic records.
- CSV clipboard runtime migration status: `copyTableAsCSV.ts` now routes browser clipboard writes and legacy textarea copy fallback through `copyTableAsCSVRuntime.ts` instead of reading `navigator.clipboard` or using document copy APIs directly; production clipboard and document lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, Electron clipboard bridge tests now pass an explicit Electron API scope into `copyTableAsCSV` instead of registering the renderer fallback candidate, production defaults plus explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document/navigator properties, and Electron clipboard bridge candidate typing now uses an explicit shared clipboard method alias instead of deriving from the monolithic `ElectronAPI` type or carrying a local interface.
- App performance utility runtime migration status: `performanceUtils.ts` now routes debounce, throttle, batch, and idle-callback timing through `electron-app/utils/app/performance/performanceUtilsRuntime.ts` instead of calling timer, idle-callback, or clock globals directly; production timer, idle-callback, and clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and browser-runtime timer aliases, and explicit runtime scopes must now provide timer, idle-callback, and clock providers instead of direct properties or falling back to `globalThis`, `Date.now`, or direct `performance` properties. Shared `PerformanceMonitor` start/end timing now routes through `electron-app/utils/performance/performanceMonitorRuntime.ts` instead of calling `performance.now` directly. Lazy-rendering viewport reads, element checks, observer creation, animation/idle callbacks, and timeout fallback scheduling now route through `electron-app/utils/app/performance/lazyRenderingRuntime.ts`; production document, HTMLElement, IntersectionObserver, viewport, animation-frame, idle-callback, and timeout defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and aliases, and explicit runtime scopes must provide their own browser primitives through named providers instead of direct ambient runtime types, direct properties, or falling back to `globalThis`.
- Async cancellation runtime migration status: `cancellationToken.ts` now routes timeout-backed token cancellation and cancellable delay scheduling through `electron-app/utils/app/async/cancellationTokenRuntime.ts` instead of calling timer globals directly; production timer defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and timer aliases, explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct timer properties, or direct ambient timer types, and explicit runtime scopes must now provide timer primitives through providers instead of falling back to `globalThis`.
- State integration runtime migration status: performance-monitoring interval and persistence debounce handles now stay in `electron-app/utils/state/integration/stateIntegration.ts` module state instead of `__performanceMonitoringInterval` or `__persistenceTimeout` globals, and state-integration storage, performance-memory, clock, interval, and debounce access now route through `electron-app/utils/state/integration/stateIntegrationRuntime.ts` instead of calling runtime globals directly inside `stateIntegration.ts`; production defaults and explicit timer/storage/performance scopes stay behind named provider functions and shared browser-runtime timer aliases rather than per-property global getters, direct ambient timer types, or direct scope properties, and explicit state-integration runtime scopes must now provide timer and clock primitives instead of falling back to `globalThis` or `Date.now`.
- Renderer state integration runtime migration status: example subscription cleanup scheduling, cleanup, state-aware event-handler abort-controller creation, state-aware document click listener registration, reactive UI document access, and state-aware event target/content element checks now route through `electron-app/utils/state/integration/rendererStateIntegrationRuntime.ts` instead of calling timer globals, constructing `AbortController`, registering document listeners, reading/writing the document, or checking ambient DOM constructors directly inside `rendererStateIntegration.ts`; production defaults and explicit timer/document/controller/constructor scopes stay behind named provider functions rather than per-property global getters, helper-level document globals, direct ambient constructor/timer types, or direct scope properties, production AbortController, timer, document, Element, and HTMLElement lookups now reuse shared browser runtime providers and aliases instead of local inline `globalThis` getters, explicit runtime scopes must now provide timer, document, document event-target, Element, and HTMLElement primitives instead of falling back to `globalThis`, and scoped renderer-state Electron API validation now uses an explicit optional-function property guard and shared preload-event method alias instead of casting the candidate preload API to a generic record or carrying a local Electron API interface.
- State integration test-global migration status: state integration unit tests now assert removed AppState, chart-controls, globalData, render-state, timer, and state-debug globals stay absent without seeding, deleting, or defining those retired global names.
- State integration browser fixture migration status: state integration tests now install and restore localStorage, performance, and performance-memory fixtures through descriptor-scoped helpers instead of assigning, reflecting directly onto `globalThis`, or deleting globals during cleanup.
- Lifecycle listener test-global migration status: lifecycle listener strict tests now assert retired lifecycle helper globals stay absent without deleting or defining `globalData`, `sendFitFileToAltFitReader`, `renderChartJS`, `copyTableAsCSV`, or `createExportGPXButton` on `globalThis`.
- Lifecycle/UI state test vocabulary migration status: lifecycle export and UI renderability tests now describe active FIT raw data instead of retired `globalData` state, with architecture coverage blocking the stale wording from returning in those focused tests.
- Lifecycle listener print fixture migration status: lifecycle listener strict tests now mock `window.print` through a Vitest spy instead of assigning `window.print` directly.
- Lifecycle listener runtime migration status: export cleanup timer scheduling/cleanup, Open File click-listener abort-controller creation, menu print dispatch, accessibility body-class updates, and test-environment detection now route through `electron-app/utils/app/lifecycle/listenersRuntime.ts` instead of calling timer globals, constructing `AbortController`, mutating `document.body.classList`, or keeping a module-level `globalThis` alias inside `listeners.ts`; chart resize-listener browser APIs route through `electron-app/utils/app/lifecycle/listenersResizeRuntime.ts`, resize-listener controller, element, canvas, timer, and animation-frame contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, and resize-listener vendor fullscreen element lookup now reads explicit document candidate fields instead of routing vendor field names through `Reflect.get`; lifecycle-listener production defaults now live in an explicit provider object instead of a broad `globalThis` default scope, process environment reads now reuse `electron-app/utils/runtime/processEnvironment.ts`, production print/default AbortController/timer/document/URL lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, direct ambient controller/timer types, or generic `Reflect.get(globalThis, ...)` probes, malformed vendor fullscreen fields fail closed, explicit scopes use named provider functions instead of direct scope properties, and explicit runtime scopes must provide timer, document, URL, print, and process-environment primitives instead of falling back to `globalThis`.
- Alt FIT sender runtime migration status: Alt FIT iframe lookup, logging, location reads, and load-listener abort-controller creation now route through `electron-app/utils/files/import/altFitSenderRuntime.ts` instead of probing global defaults or constructing `AbortController` directly inside `sendFitFileToAltFitReader.ts`; production defaults stay behind named provider scope functions instead of a broad `globalThis` default scope, production AbortController, console, document, and location lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, and explicit runtime scopes must provide providers instead of direct scope properties.
- Menu modal presenter migration status: menu-triggered accent-color and keyboard-shortcuts presenters now cache imported module exports inside `menuIpcListeners.ts`, and the keyboard-shortcuts modal no longer publishes open/close helpers on the global object.
- Keyboard-shortcuts modal test-global migration status: keyboard-shortcuts modal unit tests now assert `showKeyboardShortcutsModal` and `closeKeyboardShortcutsModal` stay absent from `globalThis` without deleting those retired compatibility names before module import.
- Keyboard-shortcuts modal runtime migration status: keyboard-shortcuts modal close timers, show animation-frame scheduling, DOM creation/query operations, body/head append operations, active-element lookup, element/event constructor checks, and body-scroll state updates now go through `keyboardShortcutsModalRuntime.ts` instead of calling timer, animation-frame, document/body/head, or browser-constructor APIs directly inside `keyboardShortcutsModal.ts`; production defaults now reuse shared browser runtime providers and browser-runtime type aliases for timers, animation frames, document lookup, and element/event constructors, and explicit test scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct timer/animation-frame/document/constructor scope properties, or direct ambient constructor/timer/frame types.
- Settings modal presenter migration status: settings modal open/close helpers now stay as module exports and are no longer published as `globalThis.showSettingsModal` or `globalThis.closeSettingsModal`.
- Settings modal test-global migration status: settings modal unit tests now assert presenter helpers stay absent from `globalThis` without deleting retired `showSettingsModal` or `closeSettingsModal` globals during fixture setup or cleanup.
- Settings modal runtime migration status: settings modal close timers, show animation-frame scheduling, modal/style lookup, element/SVG creation, body/head append operations, active-element lookup, document keydown listener target lookup, and browser event/element constructor checks now go through `settingsModalRuntime.ts` instead of calling timer, document, or browser-constructor globals directly inside `settingsModal.ts`; production defaults now reuse shared browser runtime providers and browser-runtime type aliases for timers, animation frames, document lookup, and element/event constructors, including shared HTML input/select constructor providers, and explicit test scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct timer/document/constructor scope properties, or direct ambient constructor/timer/frame types.
- Renderer notification runtime migration status: notification queue state timestamps, animation frames, frame cancellation, auto-hide timers, and serialized display timers now go through `showNotificationRuntime.ts` instead of calling `Date.now`, `setTimeout`, `clearTimeout`, `requestAnimationFrame`, or `cancelAnimationFrame` directly inside `showNotification.ts`; production defaults and explicit test scopes stay behind named provider functions instead of direct clock/timer/animation-frame scope properties, and the default browser frame, clock, and timer providers now reuse shared browser runtime providers and browser-runtime type aliases without helper-level ambient frame captures, a module-level `browserGlobal` alias, a direct optional browser-global shape, or direct ambient timer/frame types.
- Render notification runtime migration status: chart render-notification decision timestamps now go through `showRenderNotificationRuntime.ts` instead of calling `Date.now` directly inside `showRenderNotification.ts`; production date-clock defaults now reuse the shared browser runtime provider, and production defaults plus explicit runtime scopes stay behind named provider functions instead of local `Date.now` getters, direct clock properties, or broad global scope fallbacks.
- Renderer notification DOM migration status: notification host lookup, reset/clear lookup, content container creation, icon/message element creation, action button creation, and persistent close-button creation now go through `showNotificationRuntime.ts` instead of querying or creating through `document` directly inside `showNotification.ts`; production document, HTMLElement, and KeyboardEvent defaults now reuse shared browser runtime providers and browser-runtime constructor aliases, and explicit runtime scopes stay behind named provider functions instead of direct properties, a broad `globalThis` fallback scope, or direct ambient constructor types.
- Update and synced notification timer migration status: update-notification auto-hide timers, renderer-state synced notification hide timers, and synced-notification state timestamps now go through `notificationTimerRuntime.ts` instead of calling `Date.now`, `setTimeout`, or `clearTimeout` directly inside their notification modules; production date and timer defaults now reuse `electron-app/utils/runtime/browserRuntime.ts` providers and aliases, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct ambient runtime types, or direct clock/timer properties.
- Update notification DOM migration status: update-notification element lookup and action/message element creation now go through `electron-app/utils/ui/notifications/showUpdateNotificationRuntime.ts` instead of querying or creating through `document` directly inside `showUpdateNotification.ts`; production document lookup now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes stay behind named document provider functions instead of direct properties or a broad `globalThis` fallback scope.
- Chart settings render runtime migration status: chart settings fallback render-request events and fallback chart container/body document access now route through `electron-app/utils/app/initialization/chartSettingsRenderRuntime.ts` instead of constructing `CustomEvent` instances, querying chart containers through `document`, or falling back to `document.body` directly inside `chartSettingsRender.ts`; production CustomEvent, document, and event-target defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, the CustomEvent contract reuses the shared browser-runtime constructor alias instead of a direct ambient constructor type, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct `CustomEvent`/event-target/document properties.
- Summary storage-key migration status: summary column preferences now resolve file identity only through active FIT metadata or the provided data object instead of falling back to `window.activeFitFileName`.
- Summary storage-key test-global migration status: render-summary helper tests now prove default-key behavior without creating or deleting the retired `window.activeFitFileName` fallback.
- Main UI summary selector runtime migration status: summary-tab DOM lookup, gear-button DOM lookup, and delayed gear-button scheduling now route through `electron-app/renderer/mainUiSummaryColumnSelectorRuntime.ts` instead of keeping DOM/timer access inside `mainUiSummaryColumnSelector.ts`; production document, element-constructor, and timer defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers and browser-runtime type aliases, explicit runtime scopes use named provider functions instead of a broad `globalThis` default scope or direct document/constructor/timer properties, and default document access now lives on the named default provider instead of a private `getGlobalDocument()` helper.
- Summary column modal runtime migration status: tooltip viewport reads, modal listener abort-controller creation, active-element lookup, mouse/keyboard event type checks, modal DOM/text-node creation, and body append now route through `electron-app/utils/rendering/helpers/summaryColModalRuntime.ts` instead of reading viewport globals, constructing `AbortController`, touching `document`/`HTMLElement`, or checking `MouseEvent`/`KeyboardEvent` directly inside `summaryColModal.ts`; production defaults and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document/element/event/viewport/controller properties, and the production AbortController, document, HTMLElement, KeyboardEvent, MouseEvent, and viewport lookups now reuse shared browser runtime providers and AbortController/HTMLElement constructor aliases instead of local inline `globalThis` getters or direct ambient constructor type spellings.
- Render-summary runtime migration status: summary container lookup, summary gear-button/SVG creation, summary render and virtualized lap-row cleanup abort-controller creation, filter/table/virtual-row DOM creation, document-fragment creation, summary column preference storage reads/writes/removals, virtualized summary lap-row scheduling, and resize invalidation now go through `renderSummaryRuntime.ts` instead of querying/creating through `document`, constructing `AbortController`, reading/mutating `localStorage` directly, or probing `globalThis.requestAnimationFrame`, `globalThis.cancelAnimationFrame`, or `globalThis.addEventListener` directly inside `renderSummary.ts` or `renderSummaryHelpers.ts`; production defaults now reuse shared browser runtime providers for AbortController, resize listener registration, animation frames, localStorage, and document lookup, render-summary provider contracts now reuse shared browser-runtime controller/listener/frame aliases instead of direct ambient constructor and function type spellings, and explicit test scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document, controller, listener, storage, or animation-frame scope properties.
- Create-tables runtime migration status: default Data tab container lookup now routes through `electron-app/utils/rendering/components/createTablesRuntime.ts` instead of querying `document` directly inside `createTables.ts`; the production document default now reuses the shared browser runtime provider instead of a private `getGlobalDocument()` helper or local inline `globalThis` getter, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document properties.
- Render-table runtime migration status: Data tab table DOM creation, style reads, element type checks, animation-frame scheduling, and timer scheduling/cleanup now route through `electron-app/utils/rendering/core/renderTableRuntime.ts`; production defaults now reuse shared browser runtime providers for document, computed-style, element constructors, animation frames, and timers, render-table timer and HTMLElement contracts now reuse shared browser-runtime aliases instead of direct ambient timer-handle or HTMLElement constructor type spellings, DataTables constructor validation now uses explicit static-marker property guards instead of casting dependency candidates to partial constructor shapes, and explicit render-table runtime scopes stay behind named provider scope functions instead of a broad `globalThis` default scope, local inline `globalThis` getters, or direct scope properties.
- Controls-state runtime migration status: controls-state DOM lookups and computed-style reads now route through `electron-app/utils/rendering/helpers/updateControlsStateRuntime.ts`; production document and computed-style defaults now reuse shared browser runtime providers instead of local inline `globalThis` getters, and architecture coverage rejects ambient document/computed-style getter fallbacks.
- Loading overlay runtime migration status: loading-overlay DOM creation, SVG creation, body append, and selector lookup now route through `electron-app/utils/ui/components/LoadingOverlayRuntime.ts`; the production document default now reuses the shared browser runtime provider instead of a local inline `globalThis` getter, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document properties.
- Renderer loading sync runtime migration status: loading-overlay lookup, interactive form-control queries, body loading attributes, and disableable form-control checks now route through `electron-app/utils/ui/loading/syncRendererLoadingRuntime.ts`; production document and form-control constructor defaults now reuse shared browser runtime providers and constructor aliases instead of local inline `globalThis` getters or direct ambient constructor types, and explicit runtime scopes must provide document and HTML element constructor providers instead of direct properties or `document.defaultView` fallbacks.
- Fit Browser feature-gate Electron API migration status: `initFitBrowserFeatureGate` now accepts an explicit renderer Electron API scope, so feature-gate tests no longer register a shared renderer fallback candidate to read Browser enabled-state IPC, and its candidate typing now uses the shared Fit Browser API-domain contract instead of deriving from the monolithic `ElectronAPI` type.
- Tab rendering manager runtime migration status: render recency checks, render duration timing, and completed-render timestamp writes now route through `electron-app/utils/ui/tabs/tabRenderingManagerRuntime.ts` instead of calling `Date.now` or `performance.now` directly inside `tabRenderingManager.ts`; production date-clock and performance defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, and explicit runtime scopes must provide clock providers instead of direct properties or ambient fallbacks.
- Tab readiness state runtime migration status: tab readiness timestamps now route through `electron-app/utils/ui/tabs/tabReadinessStateRuntime.ts`; production date-clock defaults reuse the shared browser runtime provider instead of a local `Date.now` getter, and explicit runtime scopes stay behind named provider functions instead of direct clock properties or broad global fallbacks.
- External-link handler runtime migration status: browser-window opening, delegated keyboard-event checks, and external-anchor resolution now route through `electron-app/utils/ui/links/externalLinkHandlersRuntime.ts` instead of reading `window.open` directly or checking ambient `KeyboardEvent`, `Element`, or `HTMLAnchorElement` constructors inside `externalLinkHandlers.ts`; production defaults now reuse shared browser runtime providers and constructor aliases rather than local per-property `globalThis` getters or direct ambient constructor types, explicit runtime scopes must use providers instead of direct constructor or `open` properties, and external-link Electron bridge candidate typing now uses an explicit shared shell-external method alias instead of deriving from the monolithic `ElectronAPI` type or carrying a local interface.
- Shown-files list runtime migration status: shown-files viewport reads, body theme listeners, body theme-class checks, mousemove listeners, tooltip DOM creation/query/body append, timer scheduling/cleanup, and listener abort-controller creation now route through `electron-app/utils/rendering/components/shownFilesListRuntime.ts`; production defaults now live in an explicit provider object instead of a broad `globalThis` default scope, shown-files production AbortController, timer, document, event-target, HTMLElement, and viewport lookups now reuse shared browser runtime providers instead of local inline `globalThis` or viewport getters, shown-files timer/controller/HTMLElement provider contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, and explicit runtime scopes must provide timer, viewport, event-target, document, and AbortController providers instead of direct properties or `globalThis` fallbacks.
- Show FIT data render-state migration status: `showFitData` no longer accepts the deprecated `resetRenderStates` option; render-state resets are owned by `AppActions` and typed renderer state facades.
- Show FIT data runtime migration status: post-load map-container detection, scroll availability checks, reduced-motion media queries, microtask scheduling, scroll calls, and FIT-file-loaded event dispatch now route through `electron-app/utils/rendering/core/showFitDataRuntime.ts` instead of probing `document`, `globalThis.scrollTo`, `globalThis.matchMedia`, dispatching `CustomEvent` instances, or queueing microtasks directly inside `showFitData.ts`; production CustomEvent, document, dispatchEvent, matchMedia, queueMicrotask, and scrollTo defaults now reuse shared browser runtime providers instead of local inline `globalThis` getters, FIT data CustomEvent, dispatchEvent, matchMedia, and queueMicrotask provider contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, and explicit runtime scopes must provide providers instead of direct scope properties.
- Main UI DOM utility Electron API migration status: `validateElectronAPI` now accepts an explicit renderer Electron API scope, so main UI DOM utility tests no longer register a shared renderer fallback candidate for FIT decoder capability checks, and FIT decoder candidate typing now uses an explicit readonly method alias backed by the shared file API-domain contract instead of deriving from the monolithic `ElectronAPI` type or carrying a generic local interface.
- Main UI composition migration status: `electron-app/main-ui.ts` is now only the renderer entrypoint/export bridge and delegates startup coordination to `electron-app/renderer/mainUiStartup.ts`; startup validates preload API access through `electron-app/renderer/mainUiElectronApi.ts`; delegates theme synchronization to `electron-app/renderer/mainUiThemeSync.ts`; delegates FIT unload cleanup and unload listener registration to `electron-app/renderer/mainUiUnloadFlow.ts`, with unload operation IDs now reading time through `electron-app/renderer/mainUiRuntimeEnvironment.ts` instead of calling `Date.now` directly; delegates summary selector registration to `electron-app/renderer/mainUiSummarySelectorRegistration.ts`; delegates external-link lifecycle to `electron-app/renderer/mainUiExternalLinks.ts`; delegates shutdown cleanup to `electron-app/renderer/mainUiShutdown.ts`; delegates menu injection and development cleanup to `electron-app/renderer/mainUiDevelopmentActions.ts`; delegates drag/drop construction to `electron-app/renderer/mainUiDragDropStartup.ts`; delegates state startup logging to `electron-app/renderer/mainUiStateStartup.ts`; delegates core vendor/fullscreen startup to `electron-app/renderer/mainUiVendorStartup.ts`; and renderer entrypoint runtime capture now resolves the default window through a named provider scope in `electron-app/renderer/runtimeEnvironment.ts` instead of a broad `globalThis.window` default parameter.
- About modal development helper migration status: modal sample/reset/animation helpers now stay behind the typed `aboutModalDevHelpers` module export in `electron-app/utils/ui/modals/aboutModal.ts` instead of being published as `aboutModalDevHelpers` on the global object.
- About modal runtime migration status: about modal show/hide animation frames, focus timers, hide timers, copy-feedback timers, content element creation, modal/system-info lookup, active-element lookup, document event-target lookup, injected-body parsing, sanitizer tree walking, and browser constructor checks now go through `aboutModalRuntime.ts` instead of calling timer, document, parser, node-filter, or browser-constructor globals directly inside `aboutModal.ts`; production defaults now reuse shared browser runtime providers and browser-runtime type aliases for timers, animation frames, document lookup, parser, node-filter, and element/event constructors, explicit test scopes stay behind named provider functions instead of a broad `globalThis` default scope, direct timer/document/parser/constructor scope properties, or direct ambient constructor/timer/frame types, and strict about-modal tests now mock that runtime instead of stubbing browser globals.
- Renderer debug logging-state migration status: chart background, zoom-reset, and animation logging helpers now read renderer debug logging through `electron-app/utils/debug/rendererDebugLoggingState.ts` and renderer availability through `electron-app/utils/debug/rendererDebugRuntime.ts` instead of probing `window.__renderer_dev` or `globalThis.window` directly, with production defaults kept behind a named provider scope function instead of a broad `globalThis` default scope, and animation logging, strict renderer startup, and renderer development debug tests no longer seed, delete, or otherwise mutate retired renderer debug globals.
- Animation debug logging runtime migration status: throttled and performance animation debug logging now resolve `Date.now` and `performance.now` through `electron-app/utils/debug/lastAnimLogRuntime.ts` instead of reading clock globals directly in `lastAnimLog.ts`; production clock defaults now reuse `electron-app/utils/runtime/browserRuntime.ts`, default performance lookup binds `performance.now` to the provided performance object without a partial-global cast, explicit runtime scopes must provide clock providers instead of direct properties or falling back to ambient clocks, and architecture coverage rejects ambient performance getter fallbacks.
- Logging test-hook migration status: `logWithLevel` now tracks Object.keys failure-test state inside `electron-app/utils/logging/logWithLevel.ts` module state instead of publishing `__vitest_object_keys_allow_throw` on the global object, and the Vitest Object.keys guard now rethrows for the logger failure path without recreating that global harness flag or writing a `__isObjectKeysWrapper` marker property on wrapper functions.
- Vitest document-guard migration status: the per-Document native-method cache in `tests/vitest/setupVitest.mjs` now stays in module-local `WeakMap` state instead of publishing `__vitest_doc_native_methods` on `globalThis`.
- Vitest document realignment migration status: setup-level jsdom document restoration now goes through the descriptor-scoped `setRuntimeDocument()` helper instead of assigning `globalThis.document` or `curWin.document` directly.
- Vitest Electron mock factory migration status: the unused `createElectronMocks` global helper has been removed from `tests/vitest/setupVitest.mjs`; tests should use explicit `vi.mock(...)` factories or local fixtures.
- Vitest Web Storage setup migration status: opaque-origin `localStorage`/`sessionStorage` hardening now lives in the dedicated `tests/vitest/shims/nodeWebStorage.ts` shim instead of an inline `StorageMock`, `ensureSafeLocalStorage`, or `ensureSafeSessionStorage` block in setup, and architecture coverage keeps direct setup storage fallback assignments from returning.
- Vitest timer/listener tracking migration status: setup-level timeout, interval, and DOM listener tracking now stays in module-local state instead of publishing `__vitest_tracked_timeouts`, `__vitest_tracked_intervals`, `__vitest_tracked_dom_listeners`, or `__vitest_timers_wrapped` on `globalThis`, and timer wrapper installation goes through a descriptor-scoped helper instead of assigning `globalThis.setTimeout`, `globalThis.setInterval`, `globalThis.clearTimeout`, or `globalThis.clearInterval` directly.
- Vitest dist resolver migration status: the generated-runtime resolver install guard now stays in `tests/vitest/setupVitest.mjs` module state instead of publishing `__fitFileViewerVitestDistResolverInstalled` on `globalThis`.
- Vitest event-listener wrapper migration status: setup-level event-listener duplicate-wrap tracking now uses module-local `WeakSet` state instead of writing `__vitest_wrapped` marker properties on wrapper functions.
- Vitest navigation-history migration status: setup-level Location navigation history now stays in module-local `WeakMap` state instead of writing `__ffvNavigationHistory` on the jsdom `Location` object.
- Vitest manual-mock registry migration status: setup cleanup comments and renderer registration fixtures no longer preserve the retired shared manual-mock registry as an ambient global test concept.
- Renderer development debug helper migration status: renderer startup now creates module-owned development debug tools without carrying the legacy appState handle or publishing `__renderer_dev`, `__renderer_debug`, `__sensorDebug`, or `__debugChartFormatting` on the global object.
- Renderer development debug runtime migration status: renderer development runtime metadata now reads location, navigator, and performance-memory records through `electron-app/renderer/developmentDebugToolsRuntime.ts` instead of probing `globalThis` directly inside `developmentDebugTools.ts`; production metadata defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers instead of local per-property `globalThis` getters, and explicit scopes use named provider functions instead of direct metadata properties.
- Renderer debug logging runtime migration status: debug-log gating now uses an explicit renderer-scope provider in `electron-app/utils/debug/rendererDebugRuntime.ts` instead of resolving `globalThis.window` through the runtime facade, and the production renderer-scope check reuses the shared browser document provider instead of probing `globalThis` directly; explicit runtime scopes must use that provider instead of direct renderer-scope properties.
- Renderer notification runtime migration status: notification timestamp reads, animation-frame scheduling, and cancellation now route through explicit runtime function providers in `electron-app/utils/ui/notifications/showNotificationRuntime.ts` instead of resolving a partial `window` scope or reading `Date.now` in the notification source; production defaults reuse shared browser runtime providers for frame, clock, timer, document, HTMLElement, and KeyboardEvent access and no longer expose `globalThis.window`.
- Gyazo startup timer migration status: deferred OAuth startup timer cleanup now stays in `electron-app/main/app/gyazoStartupTimerState.ts` module state instead of `__ffvGyazoStartupTimer` on the global object.
- Main-process app-state path typing migration status: `electron-app/main/state/appState.ts` now exposes typed overloads for known main-process state paths such as `loadedFitFilePath`, `mainWindow`, updater status, Gyazo server state, app quit state, and geolocation permission state while preserving the generic fallback for dynamic paths; window startup and main lifecycle dependencies now consume path-specific state accessors instead of local broad app-state unions, loaded-file casts, and main-window casts.
- Main-process test priming migration status: test retry and keepalive timers now stay in `electron-app/main/runtime/primeTestEnvironment.ts` module state and are cleared through `clearPrimeTestEnvironmentTimers()` instead of `__ffvTestRetryTimers` or `__ffvTestKeepalive` on the global object, Electron priming now resolves through `electronAccess` override/runtime APIs instead of reading `__electronHoistedMock` directly, and probe-listener tracking uses module-owned `WeakSet` state instead of `__ffvTestProbeInstalled` marker properties on Electron app objects.
- Main-process session handler marker migration status: duplicate session permission/download handler registration now uses module-owned `WeakMap` tracking in `electron-app/main/app/setupApplicationEventHandlers.ts` instead of `__ffvSessionPermissionHandlersRegistered` or `__ffvSessionDownloadHandlersRegistered` marker properties on Electron session objects.
- Resource manager migration status: cleanup/resource tracking now stays behind the typed `electron-app/utils/app/lifecycle/resourceManager.ts` module exports instead of publishing `resourceManager` on the global object, unload cleanup listener registration now goes through `resourceManagerRuntime.ts` instead of probing `globalThis.window`, `window.addEventListener`, or `AbortController` directly in `resourceManager.ts`, registered timer cleanup now clears timers through that runtime instead of calling `clearTimeout` directly, resource registration timestamps now read from the runtime clock provider instead of calling `Date.now` directly, and specialized chart/map/observer/worker cleanup validation now uses explicit cleanup-method candidate property reads instead of routing known method names through a generic `Reflect.get` helper; production timer, clock, and AbortController lookups now reuse shared browser runtime providers and browser-runtime type aliases, production defaults stay behind named event-target/controller/timer/clock providers instead of a broad `globalThis` default scope or local inline global getters, invalid non-function specialized cleanup markers fail closed, and explicit runtime scopes must use those providers instead of direct timer, event-target, controller, or clock properties.
- App-actions lifecycle timing migration status: load-file performance timestamps plus chart/map/table render duration measurements now route through `electron-app/utils/app/lifecycle/appActionsRuntime.ts` instead of calling `Date.now` or `performance.now` directly inside `appActions.ts`; production date-clock and performance defaults now reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, and focused runtime coverage plus architecture coverage block direct timing globals and legacy direct runtime-scope properties from returning.
- Legacy app-state handle migration status: `electron-app/utils/state/domain/appState.ts` has been removed; renderer app-domain reads and subscriptions now go through the central state manager facade.
- Renderer Electron API access migration status: master state manager version/dev/file-dialog IPC, lifecycle listener IPC wiring, renderer state file-open callback wiring, main-process state client IPC, main UI theme/menu/unload wiring, renderer startup hooks, renderer environment dev-mode detection, export Gyazo/clipboard bridging, CSV clipboard copying, external-link handling, fullscreen IPC, update-install notifications, theme setup/change synchronization, settings theme synchronization, About/system version loading, recent-files context-menu loading, renderer menu IPC, FIT-data loaded notifications, main file-open dialog handling, overlay file selection, single overlay FIT decoding, file-browser path opening, file-browser tab APIs, FIT browser feature gating, and drag/drop FIT decoding now resolve preload APIs through `electron-app/utils/runtime/electronApiRuntime.ts` instead of reading `globalThis.electronAPI` or `window.electronAPI` directly.
- Lifecycle listener Electron API contract migration status: lifecycle listener preload candidates now compose the shared file, menu-event, and preload-event API-domain contracts instead of deriving a local helper shape from the monolithic `ElectronAPI` type.
- Renderer Electron API contract guardrail status: architecture coverage now confines full `ElectronAPI` imports to the central renderer runtime accessor, so feature modules must use split preload API-domain contracts.
- Recent-files context-menu runtime migration status: recent-files menu viewport clamping, focus-delay scheduling/cleanup, listener abort-controller creation, menu lookup/creation/body attachment, attachment verification, body debug reads, outside-click document listener registration, outside-click target Node checks, and menu-age timestamp reads now read browser dimensions and route clocks/timers/controllers/document/constructor primitives through `recentFilesContextMenuRuntime.ts` instead of probing `globalThis.window`, `window.innerWidth`, or `window.innerHeight`, calling `Date.now` or timer globals, constructing `AbortController`, querying/creating through `document`, reading `document.body`, registering document listeners, or checking ambient `Node` directly inside `recentFilesContextMenu.ts`; production defaults stay behind named provider scope functions for viewport, clocks, timers, document event target, abort-controller creation, and Node checks, production AbortController, timer, clock, document, Node, and viewport lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis`, direct ambient constructor/timer types, `Date.now`, or viewport getters, explicit runtime scopes must now provide those providers instead of falling back to `globalThis`, a partial `window` scope, or direct scope properties, and scoped recent-files Electron API validation now uses explicit required/optional function property checks instead of casting the candidate preload API to a partial API record.
- File-browser tab runtime migration status: Browser tab container/status/view-button lookups, Files/Calendar/Library view path/content/control lookups, open-file button lookup, Browser tab scaffold/row/card/control element and text-node creation, HTMLElement/input/select checks, managed listener abort-controller creation, Browser listing/library scan timestamp reads, and file-browser preference/cache storage reads and writes now route through `electron-app/utils/ui/browser/fileBrowserTabRuntime.ts` instead of reading those Browser tab DOM/storage primitives, constructing `AbortController`, calling `Date.now`, or touching `localStorage` directly inside `fileBrowserTab.ts`; production defaults stay behind named provider scope functions for document access, element checks, controller construction, clock reads, and storage access rather than a broad `globalThis` default scope, production AbortController, clock, document, HTMLElement, HTMLInputElement, HTMLSelectElement, and localStorage lookups now reuse shared browser runtime providers and aliases instead of local inline `globalThis`, direct ambient constructor types, `Date.now`, storage, or constructor getters, explicit runtime scopes must use those providers instead of direct properties, visible Browser loaded/scanned status timestamps now render from the same runtime timestamps recorded in Browser state/cache payloads, and Browser tab preload API candidate checks now use direct optional-function property guards instead of string-key reflective method probing.
- Browser state normalization migration status: Browser listing and scan state normalization now narrows persisted state through explicit `BrowserListingStateCandidate` and `BrowserScanStateCandidate` shapes instead of casting unknown state payloads to a broad string-indexed record, keeping Browser tab state on the typed `browser.*` branch without reintroducing legacy global-data reads.
- Unified control-bar runtime migration status: resize-listener wiring, timeout scheduling/cleanup, DOM creation/query/body access, HTMLElement checks, mutation-observer construction, and listener abort-controller creation now route through `electron-app/utils/ui/unifiedControlBarRuntime.ts` instead of using browser primitives directly inside `unifiedControlBar.ts`; production defaults stay behind named provider scope functions, production controller, timer, document, event-target, HTMLElement, and MutationObserver lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, and explicit runtime scopes must use providers instead of direct controller, timer, document, event-target, HTMLElement, or MutationObserver properties.
- FIT browser feature-gate runtime migration status: Browser tab/content DOM lookup and visibility updates now route through `electron-app/utils/ui/browser/initFitBrowserFeatureGateRuntime.ts`; production defaults and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document/HTMLElement properties, production document and HTMLElement lookups now reuse shared browser runtime providers and the shared HTMLElement constructor alias instead of local inline `globalThis` getters or direct ambient constructor types, and explicit runtime scopes must provide an `HTMLElement` constructor provider or document-owned constructor instead of falling back to `globalThis`.
- Power-estimation settings modal runtime migration status: modal listener abort-controller creation, Escape-key document listener registration, modal element creation, body attachment, and body containment checks now route through `electron-app/utils/ui/modals/openPowerEstimationSettingsModalRuntime.ts` instead of constructing `AbortController`, registering document listeners, creating elements through `document`, or reading `document.body` directly inside `openPowerEstimationSettingsModal.ts`; production defaults stay behind named provider scope functions, production AbortController and document lookups now reuse shared browser runtime providers and the browser-runtime AbortController constructor alias instead of local inline `globalThis` getters or a direct ambient constructor type, and explicit runtime scopes must provide controller and document event-target providers instead of falling back to `globalThis` or direct scope properties.
- Modal focus-trap runtime migration status: modal keydown listener target lookup, active-element lookup, and keyboard-event checks now route through `electron-app/utils/ui/modals/modalFocusTrapRuntime.ts` instead of reading `document` or `KeyboardEvent` directly inside `modalFocusTrap.ts`; production document and KeyboardEvent lookups now reuse shared browser runtime providers and the browser-runtime KeyboardEvent constructor alias instead of local inline `globalThis` getters or direct ambient constructor types, and production defaults plus explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct document/constructor properties.
- Renderer Electron API runtime registration status: renderer startup now captures the current preload API candidate through the runtime environment and passes it through explicit provider scopes without installing a global `electronAPI` reassignment proxy; `getRendererElectronApi` resolves explicit provider scopes first and otherwise uses the centralized `getBrowserElectronApiCandidate` provider instead of reading `window.electronAPI`, casting `globalThis`, or owning a feature-local fallback candidate. Application startup resolves startup hooks from the renderer entrypoint's captured preload candidate, startup hook defaults and explicit runtime scopes resolve through the named `getElectronApiScope` provider instead of broad `globalThis` default parameters, raw `RendererElectronApiScope` arguments, or direct `electronAPI` scope properties, and focused FIT-file import, complete FIT-file import, colocated FIT-file import, known-path FIT import, CSV clipboard export, chart clipboard export, export utility flows, colocated export utility flows, Gyazo export UI, Gyazo OAuth state, file-browser accessibility, FIT browser feature gating, fullscreen controls, main UI startup/strict flows, lifecycle listener coverage and strict flows, master state manager coverage, external-link handling, keyboard shortcuts modal and menu IPC, settings modal, About modal external links, version info initialization, FIT-data loaded notifications, renderer state file-open callbacks, renderer menu IPC, app listener IPC, update notifications, overlay selection/fallback, recent-file, lifecycle, drag/drop, theme setup, and theming regression tests provide scoped Electron API fixtures instead of mutating `globalThis.electronAPI`.
- Renderer Electron API startup-hook migration status: startup hook extraction now uses a typed Electron API hook-source guard and direct callable checks instead of normalizing the preload API candidate through a generic `toModuleRecord` helper.
- Renderer Electron API menu-callback migration status: Electron menu action wiring now owns the exported local `RendererElectronMenuCoreModules` `applyTheme`/`showAboutModal` dependency contract and calls those functions directly, while Electron API wiring reuses that shared menu contract instead of importing or re-declaring the broader `RendererCoreModules` pick; theme-change and About menu callbacks no longer route through the generic renderer `callUnknownFunction` bridge.
- Renderer Electron API runtime test fixture migration status: Electron API runtime unit tests now prove explicit scoped providers cover isolated lookup behavior while the default runtime reads Vitest-scoped `electronAPI` stubs only through the centralized browser-runtime candidate provider.
- Renderer Electron API window fallback migration status: `getRendererElectronApi` no longer reads `window.electronAPI`, casts ambient `globalThis.electronAPI`, or uses a registered fallback candidate; renderer startup passes the captured preload API candidate through explicit scopes, tests that need isolated lookup behavior can provide an explicit scoped provider, and the only production browser-global candidate reader is the centralized `getBrowserElectronApiCandidate` provider.
- Renderer runtime environment migration status: `electron-app/renderer/runtimeEnvironment.ts` now exposes a narrow `rendererEventTarget` handoff instead of exporting generic `scope`, `windowTarget`, or broad `rendererGlobal` handles from the runtime environment facade; event listener, timer, console, document, Electron API candidate, and renderer event-target access now resolve through named provider functions instead of accepting a direct `Window & globalThis` scope shape; listener and timer contracts reuse shared `browserRuntime.ts` aliases instead of direct `Window[...]` method type spellings; the former `getRendererScope` provider and private whole-renderer-object default helper have been removed from the runtime-environment scope contract; Electron API capture goes through `getElectronApiCandidate`, with the production browser-global reader centralized in `browserRuntime` instead of local renderer/main-UI browser-runtime casts, remaining downstream startup modules receive the renderer event target and global function contracts through explicit renderer entrypoint mappings plus named runtime-environment type exports instead of spelling `typeof globalThis.*` in their own option contracts, and renderer lifecycle cleanup now narrows core-module cleanup shapes through explicit app-action/state-manager contracts instead of flattening unknown modules to a broad record.
- Renderer browser runtime scope migration status: `createRendererRuntimeEnvironment` and `getMainUiRuntimeEnvironment` now require explicit runtime scopes; production browser global reads moved behind shared `browserRuntime` providers while the pure environment factories and renderer entrypoints stay free of silent default `globalThis` fallback binding. Split vendor-loader defaults for renderer event listeners, timers, DOM access, DOM constructors, `CustomEvent`, clock reads, and AbortController creation now reuse the named renderer browser runtime providers instead of local per-property `globalThis` or `Date.now` getters; split vendor readiness-event dispatch, vendor map document/global cleanup defaults, startup performance timing, renderer development debug metadata, and main-UI summary selector DOM/timer defaults also reuse the named renderer browser runtime providers. The assembled renderer and main-UI runtime-environment default scopes now route their document, timer, console, event-target, clock, and Electron API candidate defaults through named shared runtime providers instead of embedding inline `globalThis.document`, bound timer/listener, `globalThis.console`, local `globalThis.electronAPI` casts, or `Date.now()` closures inside the scope factories. Remaining browser-global access for compatibility-only values is narrowed: preload `electronAPI`, runtime `process`, and Vitest import-mock candidates now use explicit browser-runtime slots, while `chartOverlayColorPalette` now resolves through the typed palette module and renderer development-mode inspection uses focused input contracts instead of generic `Reflect.get` probes.
- Preload main-state bridge typing migration status: `electron-app/preload/mainStateBridge.ts` now consumes shared `MainStateChange`, listener, listen request, unlisten request, and response types from `electron-app/shared/ipc.ts` instead of carrying local raw-string path and duplicate change/callback types.
- Preload main-state invoke migration status: `electron-app/preload/mainStateApi.ts` now routes read/write main-state invoke calls through the centralized typed `createSafeInvokeHandler` service instead of calling `ipcRenderer.invoke` directly inside the state API leaf module.
- Preload main-state API factory migration status: `electron-app/preload/preloadModuleTypes.ts` now owns the concrete `CreateMainStateApi` and `CreateMainStateApiOptions` contract, allowing `electron-app/preload/preloadStateModuleLoader.ts` to return the native `createMainStateApi` import without an `as unknown` registry cast.
- Preload main-state bridge factory migration status: `electron-app/preload/preloadModuleTypes.ts` now owns the concrete `CreateMainStateBridge` and `CreateMainStateBridgeOptions` contract, allowing `electron-app/preload/preloadStateModuleLoader.ts` to return the native `createMainStateBridge` import without an `as unknown` registry cast; `electron-app/preload/mainStateBridge.ts` now validates raw main-state change event payloads before dispatching them to typed listeners.
- Preload app-info/theme factory migration status: `electron-app/preload/preloadModuleTypes.ts` now owns concrete `CreateAppInfoApi`, `CreateAppInfoApiOptions`, `CreateThemeApi`, and `CreateThemeApiOptions` contracts, allowing `electron-app/preload/preloadAppModuleLoader.ts` to return the native `createAppInfoApi` and `createThemeApi` imports without `as unknown` registry casts.
- Preload domain-loader contract migration status: `preloadModuleTypes.ts` now owns named `PreloadApiAssemblyModules`, `PreloadAppModules`, `PreloadFileModules`, `PreloadIpcModules`, `PreloadPolicyModules`, and `PreloadStateModules` contracts; the domain loader files return those explicit contracts instead of deriving local `Pick<PreloadModuleRegistry>` aliases from the broad aggregate registry.
- Preload API assembly context migration status: `PreloadApiAssemblyContext` now carries the narrow `PreloadApiAssemblyContextModules` contract instead of exposing the full `PreloadModuleRegistry`, so composed domain factories receive only app, file, IPC, policy, and state leaf modules rather than the API-assembly factory set.
- Preload API assembly input migration status: `assemblePreloadApi` now accepts `PreloadApiAssemblyInputModules`, and `createPreloadConstants` accepts `IpcBridgeCatalog` directly, keeping API assembly inputs on the domain and IPC contracts they consume instead of the full composed `PreloadModuleRegistry`.
- Electron API leaf-domain options migration status: `electronApi*Domain.ts` modules now own explicit `ElectronApi*DomainOptions` interfaces instead of deriving their parameter shape with `Pick<ElectronApiFactoryOptions, ...>`, keeping split API domains independent from the broad final API factory option bag.
- Preload app/theme factory return-contract migration status: `appInfoApi.ts` and `themeApi.ts` now return the dedicated `ElectronAppInfoApi` and `ElectronThemeApi` shared contracts directly instead of local `Pick<ElectronAPI, ...>` aliases.
- Main UI DOM utility Electron API fixture migration status: main UI DOM utility tests now use explicit scoped Electron API fixtures for validation coverage instead of ambient `electronAPI` stubs or defining/deleting `globalThis.electronAPI` directly.
- Main UI DOM utility runtime migration status: main UI DOM listener cleanup now creates abort controllers through `electron-app/utils/ui/mainUiDomUtilsRuntime.ts` instead of constructing `AbortController` directly in `mainUiDomUtils.ts`; production defaults stay behind a named provider scope rather than a broad `globalThis` default scope, the production AbortController lookup now reuses the shared browser runtime provider and browser-runtime AbortController constructor alias instead of a local inline `globalThis` getter or direct ambient constructor type, and explicit runtime scopes must use the provider instead of a direct `AbortController` property.
- Event listener manager runtime migration status: shared drag/drop target resolution and tracked listener cleanup now route through `electron-app/utils/ui/events/eventListenerManagerRuntime.ts` instead of probing `globalThis.window` or constructing `AbortController` directly in `eventListenerManager.ts`; the runtime exposes a drag/drop-specific default target accessor instead of a generic default event-target bridge, production AbortController defaults now read through the shared `electron-app/utils/runtime/browserRuntime.ts` provider seam and browser-runtime AbortController constructor alias, production defaults stay behind named event-target and abort-controller providers rather than a broad runtime scope, partial `window` scope, local per-runtime AbortController getter, or direct ambient constructor type, and explicit runtime scopes must use those providers instead of direct properties.
- Print button runtime migration status: map print button listener cleanup, DOM creation, and print dispatch now route through `electron-app/utils/files/export/createPrintButtonRuntime.ts` instead of constructing `AbortController` directly inside `createPrintButton.ts` or reading document/print globals in the feature module; production AbortController, document, and print lookups now reuse shared browser runtime providers and the browser-runtime AbortController constructor alias instead of local inline `globalThis` getters or direct ambient constructor types, and production defaults plus explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct controller/document/print properties.
- GPX export button runtime migration status: map GPX export button listener cleanup, DOM creation, object-URL access, and object-URL cleanup scheduling now route through `electron-app/utils/files/export/createExportGPXButtonRuntime.ts` instead of constructing `AbortController`, reading document/URL globals, or calling timer globals directly inside `createExportGPXButton.ts`; production AbortController, document, timeout, and URL lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters or direct ambient constructor/timer types, and production defaults plus explicit scopes use named provider functions instead of a broad `globalThis` default scope or direct controller/document/timer/URL properties.
- Renderer Electron API test-interceptor migration status: renderer startup no longer monkey-patches `Object.defineProperty`, installs an `electronAPI` accessor proxy, or polls `window.electronAPI` in Vitest manual-mock environments to detect later assignments; architecture tests block reintroducing the accessor proxy, define-property interceptor, or polling path.
- DOM helper runtime migration status: shared DOM helper listener cleanup and omitted-root query helpers now create abort controllers and resolve default documents through `electron-app/utils/dom/domHelpersRuntime.ts` instead of constructing `AbortController` or defaulting helper roots to `document` directly in `domHelpers.ts`; production AbortController and document defaults now read through shared `electron-app/utils/runtime/browserRuntime.ts` providers and the browser-runtime AbortController constructor alias, production defaults stay behind named provider scope functions instead of a broad `globalThis` default scope, local per-runtime global getters, or direct ambient constructor types, and explicit runtime scopes must use those providers instead of direct `AbortController` or `document` properties.
- Sanitize HTML allowlist runtime migration status: fallback fragment creation, DOMParser construction, element tree walking, text-node creation, and Element checks now route through `electron-app/utils/dom/sanitizeHtmlAllowlistRuntime.ts` instead of reading document, DOMParser, NodeFilter, or Element globals directly inside `sanitizeHtmlAllowlist.ts`; production defaults now reuse the shared browser runtime provider functions and browser-runtime type aliases instead of local inline `globalThis` getters or direct ambient constructor types, and explicit runtime scopes must provide document, DOMParser, Element, and NodeFilter access through those providers instead of direct properties or broad `globalThis` fallback scopes.
- Renderer vendor readiness migration status: split-entry readiness now stays in module state and is synchronized through the typed `ffv-renderer-vendor-entry-loaded` event instead of a symbol-backed `globalThis` registry; readiness event dispatch now routes through `electron-app/renderer/rendererVendorSharedRuntime.ts` instead of calling `globalThis.dispatchEvent` or constructing `CustomEvent` directly inside `rendererVendorShared.ts`; production `CustomEvent` and event-target defaults reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, and the CustomEvent contract reuses the shared browser-runtime constructor alias instead of a direct ambient constructor type; transformed Leaflet plugin modules resolve Leaflet through the typed runtime adapter instead of a separate legacy plugin runtime shim or `Symbol.for("fitfileviewer.legacyLeafletPluginRuntime")`.
- Vitest Leaflet mock migration status: the shared setup Leaflet mock no longer exposes `markerClusterGroup` for the removed markercluster package path, and architecture coverage keeps that stale plugin mock out of `tests/vitest/setupVitest.mjs`.
- Vitest Leaflet runtime migration status: `tests/vitest/setupVitest.mjs` no longer registers a default Leaflet runtime for every test; map-focused tests install explicit runtime fixtures when needed, and architecture coverage keeps setup off `setLeafletRuntime`.
- Vitest Leaflet module-mock migration status: the unused setup-level `leafletMock` object has been removed from `tests/vitest/setupVitest.mjs`; map-focused suites own their explicit Leaflet fixtures instead of inheriting a broad fake map library from global setup.
- Shown-files Leaflet fixture migration status: shown-files list unit tests now pass an explicit local Leaflet runtime fixture to `setLeafletRuntime()` instead of stashing that fixture on `window.L` or reading markers through `global.window.L`.
- Map action-button Leaflet fixture migration status: map action-button strict tests now pass their `CircleMarker` fixture through the typed Leaflet runtime instead of stashing or clearing it on `window.L`.
- Vitest window event-target fallback migration status: `tests/vitest/setupVitest.mjs` no longer fabricates `window.addEventListener`, `window.removeEventListener`, or a no-op `window.dispatchEvent`; tests rely on jsdom's real event-target APIs or focused local fixtures.
- Vitest HTMLElement bridge migration status: `tests/vitest/setupVitest.mjs` no longer assigns `window.HTMLElement` onto the Node global; jsdom-backed suites use the environment's native element constructors directly.
- Vitest console patch migration status: setup console hardening now goes through `ensureConsoleAlive()` instead of a second global block that separately patched `window.console.group`, `window.console.groupEnd`, and `window.console.groupCollapsed`.
- Vitest global setup window-console migration status: `tests/vitest/globalSetup.mjs` now only ensures the early `globalThis.console` fallback and leaves browser `window.console` alignment to per-worker setup, avoiding import-time `globalThis.window` patching before jsdom environments exist.
- Vitest window console alignment migration status: `tests/vitest/setupVitest.mjs` now aligns `window.console` through the descriptor-scoped `setConsoleObject()` helper instead of assigning `window.console` directly.
- Vitest env console warning filter migration status: `tests/vitest/env-setup.mjs` now installs its jsdom warning filters through a descriptor-scoped helper instead of assigning `console.error` or `console.warn` directly.
- Runtime-environment console fixture migration status: preload and main-UI runtime-environment unit tests now install temporary console handles through descriptor-scoped fixtures instead of assigning `globalThis.console` directly.
- Preload source execution console fixture migration status: preload source execution tests now install and restore their temporary development-log console through descriptor-scoped helpers instead of assigning `global.console` directly or deleting the console fixture during cleanup.
- Settings state-manager console fixture migration status: settings state-manager tests now mock console methods through per-test Vitest spies instead of assigning a replacement `global.console` object at module scope.
- Settings state-manager runtime fixture migration status: settings state-manager tests now feed localStorage, storage-event listener registration, and date-clock access through the mocked `settingsStateCoreRuntime` boundary instead of defining `globalThis.localStorage` or `globalThis.addEventListener` directly.
- Settings state-core runtime migration status: storage synchronization listener abort-controller creation, storage-event registration, `localStorage` lookup, exported-settings timestamps, initial `lastModified` timestamps, settings reset/update/sync `lastModified` writes, and chart-setting removal timestamps now route through `electron-app/utils/state/domain/settingsStateCoreRuntime.ts` instead of constructing `AbortController`, reading storage globals, registering storage listeners directly, or calling `Date.now` directly inside `settingsStateCore.ts` or `settingsStateHelpers.ts`; production defaults and explicit runtime scopes now use named provider functions instead of a broad `globalThis` default scope or direct scope properties, and production AbortController, storage listener, date-clock, and localStorage lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline global getters or direct ambient listener/controller types.
- Handle-open-file console fixture migration status: handle-open-file tests now use scoped Vitest console spies and one-shot throwing mock implementations instead of assigning `console.log`, `console.info`, `console.warn`, or `console.error` directly.
- Tab-state manager console fixture migration status: tab-state manager behavior tests now restore log/warn/error capture through scoped Vitest spies instead of assigning console methods back during cleanup.
- Data-point filter state-helper console fixture migration status: data-point filter state helper tests now rely on scoped Vitest console spies and mock restoration instead of assigning `console.error` directly during setup or cleanup.
- Tab-button behavior console fixture migration status: tab-button behavior tests now restore log/warn capture through scoped Vitest spies instead of assigning console methods back during cleanup.
- Chart status indicator console fixture migration status: chart status indicator tests now restore error capture through a scoped Vitest spy instead of assigning `console.error` back during cleanup.
- Preload source Electron API fixture migration status: preload development-mode and edge-case tests now capture mocked `contextBridge.exposeInMainWorld` calls in module-local exposure maps instead of clearing or restoring `electronAPI`/`devTools` globals.
- Preload Electron API factory migration status: the exposed `electronAPI` object is now composed from named app-info, clipboard, developer, diagnostics, external, file, menu, and state domain helpers, with preload API slice contracts centralized in `electron-app/shared/preloadApi.ts` instead of local `Pick<ElectronAPI, ...>` aliases; preload exposure diagnostics now classify API structure through `Object.entries(api)` instead of casting the public API through `unknown` to a generic record.
- Vitest process-nextTick guard migration status: setup process stabilization now goes through descriptor-scoped `process` and `nextTick` helpers inside `ensureProcessNextTick()` instead of assigning `globalThis.process` or `globalThis.process.nextTick` directly.
- Playwright popup fixture migration status: the map elevation popup smoke path now installs and restores its temporary `window.open` override through the original property descriptor instead of assigning or deleting `window.open` directly.
- Playwright open-file dialog fixture migration status: Electron smoke tests now store mocked `showOpenDialog` call counts and original-dialog restore handles on the installed mock function instead of writing `__ffvPlaywright*` state to the main-process global object.
- Playwright Browser status recorder fixture migration status: the Browser loading-state smoke path now stores transient status snapshots on a hidden DOM recorder element with event-based cleanup instead of writing `__ffvPlaywrightFitBrowserStatus*` state to the renderer window.
- Playwright map-theme event recorder migration status: the map-tab smoke path now stores transient `mapThemeChanged` event payloads on a hidden DOM recorder element instead of writing `__ffvPlaywrightMapThemeEvents` state to the renderer window.
- Browser/preload receiver binding migration status: shared browser timer providers now return functions bound to the browser global, and preload IPC helpers call or bind `ipcRenderer` methods with their owning renderer object instead of detaching `on`, `send`, or `invoke` before use.
- App event FIT-data test-global migration status: `tests/unit/utils/app/events.test.ts` no longer defines or deletes retired `globalData` or `loadedFitFiles` globals; listener export coverage seeds active FIT data and loaded-file state through typed domain state helpers.
- Typed FIT-data test cleanup migration status: GPX export button, chart theme listener, and user/device info tests no longer type or delete retired `globalData`/`loadedFitFiles` globals during cleanup; they rely on typed state resets and explicit active FIT data fixtures.
- User/device info box runtime migration status: user/device info box listener cleanup and top-level DOM element creation now route through `electron-app/utils/rendering/components/createUserDeviceInfoBoxRuntime.ts` instead of constructing `AbortController` or creating section/container elements directly inside `createUserDeviceInfoBox.ts`; production AbortController and document defaults now reuse shared browser runtime providers and the shared AbortController constructor alias instead of local inline `globalThis` getters or direct ambient constructor type spellings, and explicit runtime scopes stay behind named provider functions instead of a broad `globalThis` default scope or direct `AbortController`/`document` properties.
- Chart settings dropdown test fixture migration status: strict chart settings dropdown coverage now seeds the mocked state manager at `fitFile.rawData` instead of the retired `globalData` path, matching the `FitFileSelectors` source path.
- Chart settings dropdown runtime migration status: deferred dropdown setup scheduling, listener abort-controller creation, document access, and element checks now stay behind `electron-app/utils/ui/components/ensureChartSettingsDropdownsRuntime.ts`; production defaults now live in an explicit provider object instead of a broad `globalThis` default scope, chart-settings dropdown production AbortController, document, HTMLElement, and timer lookups now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters or direct ambient constructor/timer types, and explicit runtime scopes use named providers instead of direct controller/document/element/timer properties.
- Render-map strict test fixture migration status: strict render-map coverage no longer declares a window extension carrying retired `globalData` or `loadedFitFiles` globals; map tests seed loaded files through `loadedFitFilesState`.
- Render-map runtime migration status: map cleanup timers, layer-control hover timers, zoom-slider debounce timers, layout animation-frame scheduling, lap-drawing container fallback lookup, render abort-controller creation, and lap selector change-event construction now route through `electron-app/utils/maps/core/renderMapRuntime.ts` instead of calling timer/animation-frame globals, reading `document.body` directly, constructing `AbortController`, or calling `new Event("change")` directly inside `renderMap.ts`. Production defaults stay behind named provider scope functions instead of a broad `globalThis` default scope, production AbortController, document, Event, timer, and animation-frame lookups now reuse shared browser runtime providers instead of local inline `globalThis` getters, render-map controller/timer/frame contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, and explicit render-map runtime scopes must provide those primitives through named providers instead of falling back to direct scope properties or `globalThis`.
- Zone color picker runtime migration status: chart render-request events, modal element creation, active-element reads, body attachment/containment, document lookup for chart settings, body access for inline selector refreshes, Escape-key document listener registration, and element/keyboard-event type checks now route through `electron-app/utils/ui/modals/openZoneColorPickerRuntime.ts` instead of dispatching `CustomEvent` instances through `globalThis`, reading modal document primitives directly inside the picker, or checking ambient DOM constructors inside the picker; zone-color reset/apply rerender requests now resolve the chart-state manager through `chartStateManagerRegistry.ts` instead of importing the concrete singleton directly; production CustomEvent, dispatchEvent, document, HTMLElement, HTMLInputElement, and KeyboardEvent defaults now reuse shared browser runtime providers and browser-runtime constructor type aliases instead of local inline `globalThis` getters or direct ambient constructor types, and explicit runtime scopes now use named provider functions instead of a broad `globalThis` default scope, direct scope properties, or document-window fallbacks.
- Tab visibility runtime migration status: tab visibility document access, animation-frame scheduling, and fallback timer scheduling/cleanup now route through `electron-app/utils/ui/tabs/updateTabVisibilityRuntime.ts`; production document, timer, and animation-frame defaults now reuse shared browser runtime providers and aliases, and production defaults plus explicit runtime scopes use named provider functions instead of a broad `globalThis` default scope, helper-level document globals, direct ambient runtime types, or direct document/timer/frame properties.
- Tab-button helper runtime migration status: tab-button DOM queries, computed-style reads, renderer-scope checks, and HTMLElement filtering now route through `electron-app/utils/ui/controls/enableTabButtonsHelpersRuntime.ts` instead of reading `document`, `getComputedStyle`, or ambient HTMLElement directly in the helper; camelCase computed-style fallback now normalizes to CSS property names instead of probing CSSStyleDeclaration values with `Reflect.get`; production document, computed-style, and HTMLElement defaults now reuse shared browser runtime providers and browser-runtime type aliases instead of local inline `globalThis` getters, and explicit runtime scopes stay behind named provider functions instead of direct document, style, HTMLElement, or window properties.
- Tab visibility raw-data test migration status: tab visibility state coverage now lives in `updateTabVisibility.fitRawDataState.test.ts`, uses active raw FIT data naming instead of retired `globalData` callback and mock-state terminology, and installs/restores its jsdom `window`/`document` globals through descriptor-scoped helpers instead of assigning or deleting them directly.
- Tab-state manager regression fixture migration status: tab availability regression cases now call `updateTabAvailability` with `rawFitData` fixtures instead of retired `globalData` table entries.
- Tab-button integration fixture migration status: tab button state integration coverage now models FIT data only through `fitFile.rawData` instead of carrying a retired `globalData` state slot.
- Strict main UI test fixture migration status: strict main UI startup coverage no longer seeds a retired `globalData` mock-state key while exercising renderer startup flows.
- Computed state manager test fixture migration status: generic computed-state dependency coverage now uses neutral `sampleData` fixtures instead of normalizing the retired `globalData` state path.
- Next removal step: keep legacy renderer utility bridges deleted while shrinking the remaining vendor-runtime compatibility adapters toward typed imports once chart and map browser libraries are fully import-driven
- Verification gates:
  - `npm run lint:app`
  - `npm test -- tests/unit/packaging/architectureBoundaries.test.ts`
  - `npm test -- tests/unit/utils/state/domain/fitFileState.test.ts tests/unit/utils/state/core/masterStateManager.comprehensive.test.ts`
- Exit criteria:
  - Renderer bootstrap modules import the specific utility they need.
  - Tests mock typed module imports instead of mutating a shared utility object.
  - Renderer-adjacent import graphs stay out of main-process-only modules.
  - The deleted root `utils.ts` and `electron-app/utils/legacy/` bridge files stay absent.

## Vendor Runtime Adapters

- Temporary surface: split renderer vendor bundles plus the Leaflet.draw virtual runtime wrapper
- Current owners: `electron-app/renderer/rendererVendorCore.ts`, `electron-app/renderer/rendererVendorChartData.ts`, `electron-app/renderer/rendererVendorMap.ts`
- Compatibility callers: none for persistent renderer vendor globals; Leaflet.draw loads through the `fitfileviewer:leaflet-draw-runtime` virtual side-effect module with a module-scoped Leaflet import
- Current status: split by broad runtime domain; the core vendor bundle now loads through `electron-app/renderer/vendorBundleLoader.ts` during `electron-app/main-ui.ts` startup, while the chart/data and map vendor bundles load through the same typed loader when the related tab renders; `static/app/index.html` no longer shell-loads vendor JavaScript; the generic `defineMissingGlobal` shim, `__FFV_RENDERER_VENDOR_BUNDLE__` global marker, and symbol-backed split-entry registries have been removed from runtime source, and split-entry readiness is coordinated by `rendererVendorShared.ts` through module-local loaded-entry state plus a typed readiness event so separately compiled renderer module graphs can agree on loaded entries; migrated DOM sanitizing resolves DOMPurify through `electron-app/utils/dom/domPurifyRuntime.ts` registered from the core vendor readiness payload without exposing a `DOMPurify` compatibility global, migrated export ZIP creation resolves JSZip through `electron-app/utils/files/export/exportZipRuntime.ts` registered from the core readiness payload without exposing a `JSZip` compatibility global, migrated summary statistics resolve Arquero through `electron-app/utils/rendering/helpers/arqueroRuntime.ts` registered from the core readiness payload without exposing `aq` or `arquero` compatibility globals, migrated fullscreen controls resolve screenfull through `electron-app/utils/ui/controls/screenfullRuntime.ts` registered from the core readiness payload without exposing a `screenfull` compatibility global, migrated chart helpers resolve Chart.js through `electron-app/utils/charts/core/chartRuntime.ts` registered by the chart/data vendor bundle without exposing `Chart`, Chart.js zoom, Hammer, or a test-only `globalThis`/`window` fallback, migrated chart lifecycle, primary render-loop, render-completion, devtools, export, zone-chart render, chart-count, chart-settings rerender, chart-export UI, and zone-color preview paths resolve rendered Chart.js instances through `electron-app/utils/charts/core/chartInstanceRegistry.ts` without exposing a chart-instance compatibility global, migrated DataTables table rendering resolves through `electron-app/utils/rendering/core/dataTableRuntime.ts` registered by the chart/data vendor bundle without exposing a `DataTable` compatibility global or test-only `globalThis`/`window` fallback, and migrated map core rendering, base-layer, vector-layer, marker-icon, lap-drawing, plugin-control, measurement, action-button, shown-file overlay marker helpers, and Playwright map smoke paths resolve Leaflet through `electron-app/utils/maps/core/leafletRuntime.ts` without a test-only `globalThis.L`/`window.L` fallback; `rendererVendorMap.ts` registers MiniMap through an explicit constructor import on the typed Leaflet runtime, no longer bundles the disabled markercluster path, map drawing no longer threads a `markerClusterGroup` option through route/overlay marker rendering, and the map bundle registers Leaflet before loading Leaflet.draw through the `fitfileviewer:leaflet-draw-runtime` virtual side-effect module, then removes package-created `L`/`Leaflet` aliases before publishing readiness; separate `L`, `Leaflet`, and `maplibregl` aliases are gone, the old Vite package transform hook is removed, and direct MapLibre bridge calls are quarantined to `electron-app/utils/maps/layers/mapVectorLayers.ts`
- Renderer vendor asset-name migration status: generated split renderer vendor assets now use `renderer-vendor*.js/css` filenames instead of the retired `vendor-globals*.js/css` compatibility wording, while the typed loader and readiness event keep the same entry semantics.
- Renderer vendor loader runtime migration status: split-entry readiness event listeners, polling timers, clock reads, AbortController creation, and vendor script lookup/injection now route through `electron-app/renderer/vendorBundleLoaderRuntime.ts` instead of using browser globals directly in `vendorBundleLoader.ts`; all production defaults reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, vendor-loader listener/controller/script/timer contracts now reuse shared browser-runtime aliases instead of direct ambient type spellings, production defaults and explicit runtime scopes stay behind named provider scope functions rather than per-property global getters, and explicit runtime scopes must provide browser, timer, and clock primitives through named providers instead of falling back to direct properties, `globalThis`, or `Date.now`.
- Renderer vendor readiness-event migration status: split-entry readiness event detail extraction now routes through `vendorBundleLoaderRuntime.ts` instead of checking the ambient `CustomEvent` constructor directly inside `vendorBundleLoader.ts`; production defaults stay behind a named `CustomEvent` provider, and explicit runtime scopes must provide the constructor through that provider instead of a direct `CustomEvent` property.
- Renderer vendor map runtime migration status: minimap toggle icon document-element style writes and package-created `L`/`Leaflet` alias cleanup now route through `electron-app/renderer/rendererVendorMapRuntime.ts` instead of probing `globalThis.document`, deleting renderer globals directly inside `rendererVendorMap.ts`, or accepting a broad runtime global-scope provider; production document and temporary Leaflet global cleanup defaults reuse shared `electron-app/utils/runtime/browserRuntime.ts` providers, and explicit runtime scopes stay behind named provider scope functions for document access and a single focused temporary Leaflet cleanup operation instead of direct document or global-scope properties.
- Elevation profile palette migration status: `createElevationProfileButtonRuntime.ts` now resolves overlay colors through the typed `chartOverlayColorPalette` module instead of reading a `chartOverlayColorPalette` browser-global compatibility property.
- Leaflet runtime test-global migration status: Leaflet runtime unit tests now clear only the module-local runtime adapter and no longer delete retired `L` or `Leaflet` globals while proving adapter behavior; Leaflet runtime wait-loop timeout clocks and polling waits now route through `leafletRuntime.ts` environment providers instead of calling `Date.now` directly inside the wait loop, and production clock/timer defaults now reuse shared browser runtime providers instead of local `Date.now`, `setTimeout`, or `clearTimeout` getters.
- Leaflet.draw package-surface status: the installed `leaflet-draw` package exposes only `dist/leaflet.draw.js` through `main` and has no `module` or `exports` entry, so the virtual runtime wrapper remains intentional until a package or local replacement accepts an explicit Leaflet import natively.
- Next removal step: keep the Leaflet.draw virtual runtime wrapper covered by architecture and runtime bundle tests until Leaflet.draw is replaced with a package that accepts explicit imports natively
- Verification gates:
  - `npm run build:runtime-ts`
  - `npm test -- tests/unit/packaging/rendererProcessEnvironmentPolicy.test.ts`
  - `npm run test:playwright`
- Exit criteria:
  - Heavy libraries are loaded by the tab or feature that needs them.
  - Chart, map, and data-table modules use typed imports or scoped runtime adapters where practical.
  - Architecture tests block direct Chart.js constructor global lookups from runtime source.
  - Architecture tests block direct Leaflet global lookups and persistent `L` definitions in runtime source.
  - Architecture tests quarantine direct MapLibre bridge calls to the vector-layer adapter.
  - Renderer dependency inventory documents the Leaflet.draw virtual runtime wrapper and any global-only vendor.

## Runtime CommonJS Compatibility

- Temporary surface: CommonJS-style preload and runtime bridge modules
- Current owners: `electron-app/main.ts`, `electron-app/preload.ts`, `electron-app/preload/`, runtime build scripts
- Compatibility callers: packaged Electron runtime and tests that exercise generated CommonJS output
- Current status: complete at the source boundary; CommonJS compatibility is isolated to generated Electron package output because the current root package manifest still launches bundled `dist/` files through CommonJS. Domain-specific preload files exist, dev-only global exposure now lives in `electron-app/preload/developmentToolsGlobal.ts` instead of the root preload composition file, preload startup orchestration now lives in `electron-app/preload/preloadBootstrap.ts`, dependency loading is composed by `electron-app/preload/preloadModuleLoader.ts` from app, file, IPC, state, policy, and API-assembly loader modules, and the root `electron-app/preload.ts` now imports `electron-app/preload/preloadEntrypoint.ts` directly while esbuild still emits the packaged `dist/preload.js` as CommonJS for Electron; the preload source no longer owns an injected `requireModule` handoff, `electronBridge.ts` imports Electron natively, and `scripts/bundle-preload.mjs` leaves Electron as the external package boundary without rewriting preload source; `apiAssembly.ts` now consumes assembly-context and domain factories from the injected module registry instead of requiring sibling preload modules directly, source-level `module.exports` wrappers have been removed from `electron-app/preload/*.ts`, `electron-app/shared/*.ts`, `electron-app/fitParser.ts`, `electron-app/main/constants.ts`, `electron-app/main/state/appState.ts`, `electron-app/main/app/gyazoStartupTimerState.ts`, `electron-app/main/app/setupApplicationEventHandlers.ts`, `electron-app/main/runtime/primeTestEnvironment.ts`, `electron-app/main/runtime/fitParserFacade.ts`, `electron-app/main/ipc/fileReadPayload.ts`, `electron-app/main/ipc/fitIpcPayload.ts`, `electron-app/main/security/fileAccessPolicy.ts`, and `electron-app/main/security/fileAccessPolicyState.ts`; direct preload module unit tests, preload source behavior tests, and the dist-test preload module-mock fixture now import named source exports without the removed `createPreloadSourceRequire` bridge, preload shared-policy unit tests now avoid `createRequire`, FIT parser state integration now imports parser/state-manager source through typed ESM imports, the state-integration barrel imports the main-process state-manager exports natively, application event handlers import constants/app-state/timer state source natively, main-process test priming imports app-state source natively, file/FIT IPC handlers import migrated payload and file-access policy helpers natively, and the redundant main-process external URL policy facade has been removed in favor of the shared policy module; exposed `electronAPI` method assembly now lives in `electron-app/preload/electronApiFactory.ts`, API assembly now lives in `electron-app/preload/apiAssembly.ts`, shared preload runtime/module typing now lives in `electron-app/preload/preloadModuleTypes.ts` without adding a runtime dependency, shared preload domain contracts now live as named `Electron*Api` types in `electron-app/shared/preloadApi.ts` so the factory and module registry no longer duplicate local `Pick<ElectronAPI, ...>` groups, `shell:openExternal` preload validation now uses the same shared HTTPS/mailto URL policy as main-process shell handlers, FIT browser folder preload validation now rejects malformed root-folder and relative-folder paths through a shared path policy before IPC while main keeps filesystem trust and containment enforcement, FIT file path preload validation now rejects malformed or non-absolute file paths for `file:read` and recent-file path IPC through the same shared path policy used by main file-access approval checks, main-state path/operation preload validation now rejects unsafe dot paths, prototype-pollution segments, malformed segments, and overlong renderer-provided state paths through a shared policy also used by the main-process state manager, devtools menu injection now validates and normalizes theme/file-path payloads through a shared preload/main-process policy, app source no longer calls generic renderer preload IPC methods directly, the public `electronAPI` no longer exposes `invoke`, `send`, or `onIpc`, and `createPreloadEventApi` only returns `notifyFitFileLoaded` plus allowlisted update-event subscriptions
- Before-exit registry migration status: preload before-exit listener tracking now uses module-local `WeakMap` and `WeakSet` state inside `electron-app/preload/beforeExitHandler.ts` instead of `__ffv_preload_beforeExitRegistry__` on the global object or symbol-marker mutation on listener wrappers.
- Preload API assembly migration status: the former mixed `appApiDomain.ts` and combined `externalApi.ts` aggregator have been removed; `apiAssembly.ts` now composes diagnostics, system app-info/theme, clipboard, developer-menu, dialog, split shell/Gyazo external, file, IPC-event, and state domains through focused assembly modules; `preloadApiAssemblyModuleLoader.ts` now imports and returns the API assembly context and domain modules natively instead of resolving those assembly modules through the injected `requireModule` boundary or compatibility type-cast aliases.
- Preload electron API domain migration status: final exposed `electronAPI` domain shapers now live behind typed preload domain modules, leaving `electron-app/preload/electronApiFactory.ts` focused on composing already-shaped preload domains into the public API object; app-info/theme, clipboard, developer-menu, diagnostics, dialog, shell/Gyazo external, file/browser, menu/event, and main-state exposed API shapers now live in focused `electronApi*Domain.ts` modules, and the old aggregate `electronApiDomains.ts` module has been removed. Native open-file, open-folder, and overlay dialog access now flows through explicit dialog assembly/factory domains instead of the file/browser domain.
- Preload file module-loader migration status: `preloadFileModuleLoader.ts` now imports `fileApi.ts` and `fitBrowserApi.ts` natively instead of resolving those leaf modules through the injected `requireModule` boundary, and file/FIT-browser factory option contracts now live in `preloadModuleTypes.ts` so the file module loader returns native imports without compatibility casts.
- Preload state module-loader migration status: `preloadStateModuleLoader.ts` now imports `mainStateApi.ts` and `mainStateBridge.ts` natively instead of resolving those leaf modules through the injected `requireModule` boundary.
- Preload app module-loader migration status: `preloadAppModuleLoader.ts` now imports app, clipboard, devtools, external, theme, environment, and before-exit leaf modules natively instead of resolving those leaf modules through the injected `requireModule` boundary; app diagnostics, clipboard, devtools menu, Gyazo, shell external, development-tools exposure, and before-exit factory option contracts now live in `preloadModuleTypes.ts` so the app-loader entries return native imports without compatibility casts; development-tools preload timestamps now reuse the shared logging timestamp runtime instead of constructing `new Date()` directly inside the preload global exposure helper.
- Preload IPC module-loader migration status: `preloadIpcModuleLoader.ts` now imports validators, environment policy, preload event/menu APIs, Electron API exposure, IPC helpers, logger, bridge catalog, and the Electron bridge resolver natively instead of resolving those IPC modules through the injected `requireModule` boundary; IPC/menu/preload event, helper, logger, validator, Electron API exposure, and Electron bridge resolver contracts now live in `preloadModuleTypes.ts` so the IPC module loader returns native imports without compatibility casts; the resolver imports Electron natively and the preload build leaves that package at the external Electron boundary. Main-state IPC payload serialization now uses an explicit plain-object guard before recursive value inspection instead of casting unknown payload objects to a generic record.
- Preload environment policy migration status: `environment.ts` now reads the preload `NODE_ENV` and Electron runtime markers through focused process-environment and process-version contracts instead of generic `Reflect.get` probes, and malformed or unavailable process shapes fail closed for development-mode and generic IPC allowlist checks.
- Preload logger migration status: `logger.ts` now dispatches preload log levels through explicit `log`, `warn`, and `error` method reads instead of generic `Reflect.get` console probing, and malformed or throwing console method access fails closed.
- Preload module-loader migration status: `preloadModuleLoader.ts` now imports the focused app, file, state, policy, API-assembly, and IPC loader modules natively instead of resolving those loader modules through the injected `requireModule` boundary.
- Preload runtime migration status: `preloadRuntime.ts` now imports preload module loading, API assembly, and Electron API factory composition natively instead of resolving those composition modules through the injected `requireModule` boundary, the Electron API factory is assigned through its concrete `PreloadRuntime` contract without an `as unknown` adapter, and the runtime object no longer stores the preload `requireModule`.
- Preload bootstrap migration status: `preloadBootstrap.ts` now imports preload runtime creation and default runtime-environment discovery natively instead of resolving those bootstrap dependencies through the injected `requireModule` boundary; bootstrap no longer passes a preload `requireModule` to the Electron bridge resolver and no longer accepts or resolves a generic preload `globalScope`.
- Preload entrypoint migration status: `preloadEntrypoint.ts` now imports the preload bootstrap natively instead of resolving entrypoint dependencies through fallback `requireModule` path rewriting; the entrypoint no longer accepts or passes Electron's packaged `require` or a generic preload `globalScope`.
- Preload Electron bridge override migration status: preload source tests now pass explicit `electronBridgeOverride` values through `preloadEntrypoint.ts`/`preloadBootstrap.ts`, `electronBridge.ts` no longer reads the old hoisted Electron mock global, and architecture tests keep that global name out of app source.
- Main IPC handler migration status: file, FIT, browser, dialog, recent-file, clipboard, external, and info IPC handler source modules now use named exports instead of `module.exports`; direct file-access policy consumers import the migrated policy natively; file-access policy approval logging plus info-handler process version, platform, architecture, and cwd reads go through `electron-app/utils/runtime/processEnvironment.ts` instead of touching the process global directly; and `setupIPCHandlers.ts` imports those migrated handler/policy boundaries natively instead of requiring their source files.
- IPC registry migration status: `ipcSenderPolicy.ts` and `ipcRegistry.ts` now use named source exports instead of `module.exports`; the registry imports sender-policy validation natively; IPC sender-policy test mode, resources-path, and platform reads now go through `electron-app/utils/runtime/processEnvironment.ts` instead of touching the process global directly; `setupIPCHandlers.ts` plus the main-process state manager import the registry natively; and the IPC sender-policy unit test imports the Electron access override natively instead of using a `createRequire`/`requireCjs` bridge.
- Main strict-handler test migration status: strict Electron main-handler tests now import Electron access, file-access policy, filesystem, and file-system handler modules natively instead of using `createRequire`, `requireCjs`, or inline `require(...)` test bridges.
- Main renderer-send/window validation migration status: `sendToRenderer.ts` and `windowValidation.ts` now use named source exports instead of `module.exports`; `sendToRenderer.ts` imports window validation natively; and direct main-process consumers import those migrated helper boundaries natively instead of requiring their source files.
- Main theme/updater helper migration status: `getPersistedThemePreference.ts` and `setupAutoUpdater.ts` now use named source exports instead of `module.exports`; initialize-application and IPC setup source import those migrated helper boundaries natively instead of requiring their source files, `getPersistedThemePreference.ts` now reads the main-process settings store through the typed Electron-conf adapter instead of executing renderer JavaScript against `localStorage`, main-window bootstrap no longer carries a theme-only `executeJavaScript` fallback contract, and `setupAutoUpdater.ts` imports `electron-log` natively instead of resolving it through a source-level `require(...)` boundary.
- Main bootstrap/IPC setup migration status: `initializeApplication.ts`, `setupIPCHandlers.ts`, and `gyazoOAuthServer.ts` now use named source exports instead of `module.exports`; initialize application, IPC setup, and application-event setup import migrated constants/app-state/Gyazo OAuth helper boundaries natively instead of requiring their source files, and Gyazo OAuth error/server-shape validation now uses typed optional properties instead of source-level `Reflect.get` probes.
- Main IPC validation dependency migration status: clipboard, external, and filesystem IPC handlers now import `zod` natively instead of requiring the package at source level, while keeping the same validated IPC payload behavior.
- Main support-helper migration status: `logWithContext.ts`, `safeCreateAppMenu.ts`, `electronAccess.ts`, and `setupBlockedRequests.ts` now use named source exports instead of `module.exports`; already-migrated runtime consumers import logging, menu, Electron access, and blocked-request helper boundaries natively instead of requiring their source files, main-process logging timestamps now reuse the shared logging timestamp runtime instead of constructing `new Date()` directly in `logWithContext.ts`, logging console-method selection now uses typed indexed access instead of `Reflect.get(console, level)`, blocked-request webRequest validation now uses a typed optional `onBeforeRequest` property instead of `Reflect.get(value, "onBeforeRequest")`, main-process plus state/menu utility source no longer require `electronAccess.ts` directly, and `electronAccess.ts` imports the Electron package natively through the main build's external package boundary instead of owning a direct package `require("electron")` or `loadNodeModule("electron")` call; the unused Electron access default namespace export has also been removed.
- Main updater-access migration status: `autoUpdaterAccess.ts` now uses named source exports instead of `module.exports` or a default namespace shim; setup, menu, and initialize-application consumers import the updater resolver boundary natively instead of requiring its source file; Electron-updater resolves through the async native import path instead of a synchronous `loadNodeModule` or direct package `require("electron-updater")` fallback; Vitest mock detection reads test mode through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.env` directly, and reads the Vitest import-mock candidate through `autoUpdaterAccessRuntime.ts`, which now delegates to the explicit `getBrowserVitestImportMockCandidate` provider instead of probing `globalThis` in the resolver, owning a local global-property helper, or importing the generic global-property helper; updater module property reads now use typed indexed access inside the guarded lazy-getter fallback instead of a source-level `Reflect.get` probe; and the redundant Node ESM `module.exports` namespace branch has been removed because Electron-updater's `default` namespace covers the same object shape.
- Process runtime global-property migration status: `processEnvironment.ts` now routes runtime `process` reads and test/install writes through explicit `getBrowserProcessCandidate`/`setBrowserProcessCandidate` browser-runtime providers instead of owning direct `globalThis` process access, calling generic global-property helpers, or carrying a local global-property setter helper, and nested process/env property reads now use guarded typed indexed access instead of a local `Reflect.get` probe.
- Shared browser-runtime global access migration status: `browserRuntime.ts` now reads named runtime globals, including `electronAPI`, `process`, and the Vitest import-mock candidate, through explicit optional slot contracts instead of exposing a generic global-property reader, writes the process candidate through a process-specific private setter instead of a generic global-property helper, exposes Leaflet alias cleanup through the named `deleteBrowserLeafletGlobals` provider instead of a generic global-property delete export, and verifies write results through guarded typed indexed access instead of `Reflect.get(globalThis, ...)`, while keeping the define-property fallback for configurable accessor globals.
- Browser global access architecture status: package-boundary tests now scan app source with comments stripped and fail direct `window.*`/`globalThis.*` property access outside `browserRuntime.ts`, keeping browser global providers centralized instead of letting legacy renderer globals return through individual modules.
- Main Node-runtime module migration status: `nodeModules.ts` now imports Node built-ins natively for `path`, `fs`, and `httpRef`, and its generic `loadNodeModule` package-compatibility escape hatch plus unused default namespace export have been removed; file-access policy, IPC sender policy, Gyazo OAuth, application-event, menu-event, IPC setup, and Electron access source import the runtime module boundary natively instead of requiring its source file or owning separate direct package-load fallbacks.
- Main electron-conf access migration status: `electron-app/main/runtime/electronConfAccess.ts` now imports Electron-conf natively, exposes named source exports only, and reads the native `Conf` export through typed namespace access instead of a source-level `Reflect.get` probe; app state, FIT-parser integration, app-menu creation, menu event handling, and browser/info IPC handlers use that typed adapter instead of direct source-level `require("electron-conf")` calls.
- App-menu creation migration status: `createAppMenu.ts` now resolves platform-specific app-menu, reveal-label, and quit-label behavior through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.platform` directly.
- Main lifecycle setup migration status: `setupMainLifecycle.ts` now uses a named source export instead of `module.exports`, its dev/test mode checks read `NODE_ENV` plus `--dev` argv through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.env` or `process.argv` directly, and method validation now uses typed indexed access instead of a source-level `Reflect.get` probe.
- Main test-environment priming migration status: `primeTestEnvironment.ts` now reads test mode through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.env` directly, and reads Electron mock properties through typed indexed access instead of a source-level `Reflect.get` probe while preserving the Electron override fallback for test doubles.
- Main development-helper migration status: `exposeDevHelpers.ts` now uses a named source export and imports app-state helpers natively instead of requiring the app-state source file.
- Main window bootstrap migration status: `bootstrapMainWindow.ts` and `initializeMainWindow.ts` now use named source exports instead of `module.exports`; `initializeApplication.ts` imports the bootstrap boundary natively instead of requiring its source file.
- Main FIT-parser integration migration status: `fitParser.ts` now routes decode operation IDs and `FitDecodeError` ISO timestamps through `electron-app/fitParserRuntime.ts` instead of calling `Date.now` or constructing `new Date()` directly; `fitParserIntegration.ts` now uses named source exports and imports constants/logging natively; `setupIPCHandlers.ts` imports the integration helper natively instead of requiring its source file; FIT-parser state-integration timing and last-result timestamps now route through `electron-app/main/runtime/fitParserIntegrationRuntime.ts` instead of probing ambient `performance.now` or calling `Date.now` directly; FIT parser and main FIT-parser integration production date/performance defaults now reuse shared browser runtime providers instead of local inline `Date.now` or `globalThis.performance` getters; FIT-parser state-integration test reset and adapter overrides now stay in module-local state behind named test helper exports instead of `__fitParserStateAdaptersOverride` or `__resetFitParserStateIntegrationForTests` globals.
- Main-process state manager migration status: `mainProcessStateManager.ts` now uses named source exports instead of a source-level `module.exports` fallback while the state-integration barrel imports it natively; duration timing, wall-clock state timestamps, generated timestamp IDs, completed-operation cleanup timers, and deferred IPC setup retry timers now go through `mainProcessStateRuntime.ts` instead of probing `globalThis.performance`, `performance.now`, or `Date.now`, or calling timer globals directly; production timer, performance, and date-clock defaults now reuse shared browser runtime providers and timer type aliases, production defaults and explicit timer/performance/date-clock scopes stay behind named provider functions rather than per-property global getters, direct scope properties, or direct ambient timer API types, explicit runtime scopes must now provide timer and clock primitives instead of falling back to `globalThis` or ambient clocks, state-manager log prefixes now reuse `electron-app/utils/logging/loggingTimestampRuntime.ts` instead of constructing ISO timestamps with `new Date()` directly, and development-mode `NODE_ENV`/`--dev` checks now go through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.env` or `process.argv` directly. The app-facing `getAppState`/`setAppState` wrapper now accepts only `MainAppStateKnownPath` keys; dynamic dotted paths must use the lower-level `mainProcessState` manager directly instead of preserving arbitrary app-state compatibility paths.
- Renderer version-info migration status: `loadVersionInfo.ts` now resolves fallback Electron, Node, Chrome, platform, and architecture metadata through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.versions`, `process.platform`, or `process.arch` directly, and version-info candidate typing now uses explicit readonly method aliases backed by the shared app-info API-domain contract instead of deriving from the monolithic `ElectronAPI` type or a broad `Partial` bridge; version-info candidate validation now uses direct optional-function property guards instead of method-name reflection.
- Main menu-event setup migration status: `setupMenuAndEventHandlers.ts` now uses a named source export, imports constants, IPC registry, logging, file-access policy, app-state, and safe menu creation helpers natively instead of requiring those source modules, and resolves dev/test menu-injection gating plus Linux update-dialog platform checks through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.env` or `process.platform` directly.
- Main entrypoint/application-event/window-state migration status: `main.ts` now imports runtime helpers natively instead of using its `mainRequire` table or source-level `module.exports` fallback, and its test-only default export object has been removed; `setupApplicationEventHandlers.ts` now imports logging, safe menu creation, Gyazo OAuth, theme preference retrieval, window validation, and window creation helpers natively instead of requiring those source modules, and resolves resources-path plus platform checks through `electron-app/utils/runtime/processEnvironment.ts` instead of touching `process.resourcesPath` or `process.platform` directly; `windowStateUtils.ts` now uses named source exports instead of a source-level `module.exports` wrapper, validates optional `isDestroyed` callbacks through a typed candidate shape instead of `Reflect.get`, and the window bootstrap/initialization boundaries import `createWindow` natively.
- Recent-files IPC migration status: `recentFiles.ts` now uses named source exports instead of a source-level `module.exports` wrapper, and `setupIPCHandlers.ts` imports the recent-file helpers natively instead of requiring their source module.
- Root package-script migration status: root-context package CLI helper scripts now resolve package entrypoints through `import.meta.resolve`, the macOS builder dependency helper checks optional package availability through native resolution, the preload bundler imports `esbuild` natively instead of using `createRequire`/`require.resolve`, the preload bundler no longer rewrites injected `requireModule(...)` calls, and the runtime build now bundles the Electron main entry back to CommonJS at the generated `dist/main.js` boundary so typed ESM-style source can still launch under the root CommonJS package manifest; cross-workspace package resolvers stay outside this status while they intentionally resolve from another package root.
- Release verification script migration status: package-boundary tests now expand the `verify:release` and `verify:release:signed` npm script graphs and assert the full pre-release gate keeps formatting, linting, CSS linting, docs typecheck/build, audit, Vitest, Playwright, signing preflight, unsigned packaging, signed packaging, signature artifact verification, and packaged smoke checks reachable through explicit release verification scripts; dependency-validation workflow coverage also keeps the scheduled dependency rehearsal on read-only permissions, dependency path filters, app/docs installs, `release:verify`, unsigned package artifact checks, and diagnostics upload.
- Main/package test migration status: the main-process module test no longer clears CJS require cache for the ESM-imported main entrypoint, the Electron builder file-list test imports the CJS config through native dynamic import instead of `createRequire`, the env-sensitive Electron Builder identity/signing test loads a fresh config through a Node dynamic-import subprocess instead of owning a `createRequire`/`require.cache` bridge, the main-process state-manager test now uses the typed Electron access override plus native Electron mock instead of patching `global.require` for `require("electron")`, the obsolete preload debug module-cache injection test has been removed in favor of the controlled dist-preload execution tests, and the dist-preload test helper now recognizes the current bundled preload entrypoint so test workers do not rebuild `dist/` after the runtime build already produced valid output.
- CommonJS-in-ESM mock-guide status: the obsolete `docs/MOCK_COMMONJS_IN_ESM.md` guide has been removed because it documented the retired global `require()` override and `cjsMockInterop` patterns; architecture coverage now treats that guide as a deleted compatibility file.
- App-code docs migration status: FIT parser migration and preload IPC documentation now show ESM-style imports instead of `require(...)` app-code examples, while `.cjs` build-configuration examples stay documented as intentional configuration boundaries.
- Export utility reflection migration status: export Electron API candidate validation, Imgur upload response parsing, and export runtime crypto/storage candidate checks now use explicit typed property reads instead of generic `Reflect.get` probes; malformed response/runtime method shapes fail closed, and architecture coverage blocks those reflective export utility probes from returning.
- Render-table DataTables migration status: `renderTable.ts` now validates the DataTables constructor through an explicit `isDataTable` candidate property read instead of a generic `Reflect.get` probe; malformed static markers still skip enhancement, and architecture coverage blocks that renderer dependency reflection from returning.
- Next removal step: keep CommonJS compatibility isolated to generated `dist/` output until the Electron main/preload launch path can consume native ESM output directly
- Verification gates:
  - `npm run build:runtime-ts`
  - `npm test -- tests/unit/preload.dist.test.ts tests/unit/preload.distApiMethods.test.ts tests/unit/preload.distBridgeBehavior.test.ts`
  - `npm run package`
- Exit criteria:
  - Runtime build output remains packaged and smoke-tested.
  - Preload domains have narrow validated IPC surfaces.
  - Architecture tests prevent preload modules from reaching into renderer state internals.
  - Architecture tests keep app source off generic renderer preload IPC methods.
